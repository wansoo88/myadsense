---
id: 2026-07-24-12-review
to: review
rev: 1
gate: none
status: 대기(11 완료 후 하달)
---

## 목적 (왜)
검수기가 모든 초안에 `"No affiliate or advertising disclosure anywhere in the draft"` 를 지적하는데, **현재 이 사이트에는 광고도 제휴 링크도 없다.**
없는 것을 고지하라는 요구이므로 지금 상태에서는 **오탐**이다. 그러나 광고가 실제로 게재되면 이 지적은 **반드시 살아나야 한다.**
→ 항목을 **삭제하지 말고, 실제 수익화 상태에 연동**시킨다.

## 근거 (PM 실측, 2026-07-24)
- 리포지토리 전체에 `ca-pub`·`adsbygoogle`·publisher id **0건**.
- 라이브 홈 HTML 에 광고 스크립트 **0건**(`curl https://stack.utilverse.info/ | grep -c adsbygoogle` → `0`).
- `engine/content/renderer.py` 에 제휴 링크 생성 로직 **없음**.
- 사람 판단(2026-07-24): AdSense 승인·광고 게재는 아직 한참 뒤 → **지금 광고 고지를 넣으면 사실과 다른 고지**가 된다.

## 작업
- [ ] 검수 루브릭의 **광고·제휴 고지 항목을 조건부로** 바꾼다: 실제 수익화 상태일 때만 요구.
      판정 근거는 추측이 아니라 **관측 가능한 값**이어야 한다 — 예: `config/sites.yaml` 의 수익화 플래그, 또는 렌더 산출물에 광고 스크립트/제휴 링크 존재 여부.
      플래그 신설이 필요하면 **어떤 키를 어디에 둘지 제안만** 하고 PM 에 보고할 것(`sites.yaml` 은 OPS 소유).
- [ ] 광고 미게재 상태에서는 이 항목이 **반려 사유로 잡히지 않게** 한다(무시 또는 INFO 강등). ⚠️ 다른 정책 항목(F2 Privacy Policy·F3 클릭 유도 문구)은 **그대로 유지**한다.
- [ ] **잊히지 않게 못을 박을 것**: 광고 게재·AdSense 신청 시점에 이 고지가 반드시 켜지도록 `/adsense-audit` 체크리스트(또는 승인 전 점검 항목)에 명시적으로 추가.
- [ ] 오늘 4건 재검수(11) 결과에서 이 항목이 빠졌을 때 **남는 진짜 지적이 무엇인지** 다시 정리해 보고.

## 산출물
- `engine/content/reviewer.py`(네 소유) 조건부 로직
- `.claude/skills/adsense-audit/SKILL.md` 또는 그에 준하는 체크리스트에 "광고 게재 시 고지 필수" 항목
- `team/reports/2026-07-24-12-review.md`

## 완료 기준 (DoD)
- 광고 미게재 상태에서 이 지적이 **더 이상 반려를 만들지 않음**이 재검수 출력으로 증명된다.
- 광고 게재 상태를 가정한 테스트에서는 이 지적이 **다시 살아남**이 증명된다(양방향 확인).
- 승인·게재 시점 체크리스트에 항목이 남았다.

## 경계
- ⛔ **게이트 완화 아님을 스스로 증명하라.** 이 변경은 "요구를 없애는 것"이 아니라 "요구를 정확한 조건에 붙이는 것"이다. 양방향 테스트가 그 증거다.
- ⛔ 다른 루브릭 항목·임계값에 손대지 말 것.
- ⛔ 콘텐츠 생성·수정·발행 금지.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-12-review | 조건부 전환 완료 | 양방향 검증: 미게재 무지적 / 게재 지적유지 | 남은 진짜 지적: ..."
```

---

# rev 2 — 반려 (PM, 2026-07-24 23:00)

**판정: 반려.** DoD 1·2·3 은 증거로 충족됐고 설계 의도(삭제 아닌 조건화)도 지켰다. 그러나 **경계 조항 2개를 못 지켰다.**

## 반려 사유 (PM 이 코드로 직접 확인)
1. **passed 를 뒤집는 게이트 구멍** — `reviewer.py:230-232`
   ```python
   for s in (x for x in re.split(...) if x.strip()):
       if not _DISCLOSURE_PHRASE.search(s) and _OTHER_RISK.search(s):
           return False
   ```
   문장에 고지 구절이 **함께** 들어 있으면 그 문장은 `_OTHER_RISK` 검사에서 **면제**된다.
   → 고지 + 실질 리스크가 한 문장에 섞이면 지적 전체가 `issues_not_applicable` 로 빠지고,
     그게 유일한 지적이면 `passed=True, severity="none"`.
   재현(감사자): `"The draft lacks an affiliate/ad disclosure and makes a false first-person claim of hands-on testing."` → **passed False→True**.
   보고서의 안전 주장("고지 외 지적이 하나라도 남으면 판정 불변")은 **문장이 분리됐을 때만** 참이다.
2. **F2 동반 강등(ORDER 24행 위반)** — 과거 52건 리플레이에서 강등 34건 중 **9건**이 같은 문장에 `Privacy Policy` 를 담고 있어 함께 not_applicable 로 빠졌다. "보호되어 남은 4건"은 설계가 아니라 F2 언급이 **우연히 다른 문장**에 있었기 때문이다.
3. **영향 경로** — `passed` 는 `orchestrator.py:222·238` 에서 발행 큐 게이트로 쓰인다. 표기 오류가 아니라 **법적 리스크 글의 자동 발행 경로**다. 강등된 지적은 `orchestrator.py:229-231` 재생성 피드백에서도 사라져 CONTENT 가 통보받지 못한다.

## rev 2 작업
- [ ] **문장 단위 면제를 없앤다.** `_OTHER_RISK` 를 `detail + fix` **전체 텍스트**에 적용해, 다른 리스크가 어디에 있든 강등하지 않는다.
- [ ] 최소한 **`passed` 를 뒤집는 경로에서는 전체 텍스트 검사를 강제**한다. (강등 자체는 유지해도 되지만 `passed=True` 는 무리스크 텍스트에만.)
- [ ] **회귀 테스트로 못을 박는다**: 위 재현 문장 + F2 동반 문장을 케이스로 넣어 `passed` 불변·F2 잔존을 자동 검증.
- [ ] 52건 리플레이 재실행 → **F2 동반 강등 0건** 을 실측으로 제시.
- [ ] **증거를 리포에 보존**: A/B 결과(live_off/live_on)와 리플레이 스크립트가 scratchpad 에만 있어 temp 정리 시 재현 불가. `dist/review/` 또는 보고서 본문에 원문 첨부.

## DoD (rev 2)
- 고지 + 다른 리스크가 **한 문장**에 있어도 `passed` 가 뒤집히지 않음이 테스트로 증명된다.
- 52건 리플레이에서 **F2·F3 지적 강등 0건**.
- 광고 게재 상태에서는 고지 지적이 여전히 부활한다(양방향 유지).

## 경계
- ⛔ 강등 기능을 통째로 되돌리지 말 것 — 오탐 해소라는 원래 목적은 유효하다. **좁히는 방향**으로만 고친다.
- ⛔ `_SYSTEM`·severity·`ai_tells` 자동반려·quality_gate 는 계속 불가침.
