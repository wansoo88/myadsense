---
id: 2026-07-25-20-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
어제 밤 감사에서 **서로 다른 두 개의 "0편 원인"** 이 추가로 드러났다. 둘 다 OPS 소유다.

## 근거 (2026-07-25 감사·재생성 실측)
1. **예약 20:00 배치가 2026-07-16 이후 매번 실패했다.** `logs/daily_local.log`: 매 실행 `claude CLI 실패(rc=1)` 로
   시드 4개 전부 SKIP, 0편, 총 소요 20~35초(07-24: 20:00:02 시작 → 20:00:35 종료).
   ⚠️ 즉 **어제 고친 P0(argv 한도)와는 다른, 더 오래된 별개 고장**이다. 사람이 손으로 돌리면 되는데
   **스케줄러로 돌면 rc=1** → 환경 차이(PATH·로그인 세션·인증 캐시·CWD·인코딩)를 의심하라.
2. **검수기의 `ai_tells` 가 재작성 모델에게 전달되지 않는다** — `engine/orchestrator.py:231`.
   코퍼스 156건 중 재작성 프롬프트에 도달한 건 **2건뿐**(둘 다 정규식 사전필터 경로).
   → 어제 ⑤ AI 티가 7→4 로 준 것은 **하네스 한정 증강 피드백** 덕분이고 **프로덕션에서는 재현되지 않는다.**
   검수기가 "이 문장이 AI 티다"라고 말해줬는데 재작성 모델은 그걸 못 본 채 다시 쓴다.

## 작업
- [ ] **① 예약 배치 rc=1 원인 규명.** 추측 금지 — 스케줄러와 **동일한 조건**(같은 사용자·세션·환경변수)으로 재현해
      실패 지점을 특정하라. `_cli_error_detail` 로 stderr 원문을 확보할 것.
      07-16 에 무엇이 바뀌었는지 커밋·로그로 대조하라. 고칠 수 있으면 고치고, 환경 제약이면 **대안**(로그인 세션 유지 방식 등)을 제안하라.
- [ ] **② 검수기 `ai_tells` 를 재생성 피드백에 포함**시켜라(`orchestrator.py:229-231`).
      검수기가 지목한 **문장 원문**이 재작성 모델에게 그대로 가야 한다. 어떤 필드가 어떻게 전달되는지 코드로 제시.
- [ ] ③ ①이 해결되면 **오늘 20:00 배치가 실제로 생성까지 가는지** 관측 계획을 남겨라(로그 위치·판정 기준).

## DoD
- ① 실패 원인이 **재현 가능한 형태로** 특정된다(추정이 아니라 stderr·환경 대조).
- ② 재작성 프롬프트에 검수기 `ai_tells` 원문이 들어간 것을 **실제 프롬프트 문자열**로 제시(도달률 2/156 → 개선 수치).
- ③ 오늘 20:00 결과를 무엇으로 판정할지 사전에 정의돼 있다.

## 경계
- ⛔ 발행·배포 실행 금지. 진단·수정까지만.
- ⛔ `generator.py`·`reviewer.py` 수정 금지(타 소유). `orchestrator.py` 는 OPS 소유.
- ⛔ 검수 상한·판정 임계값·킬스위치·cap 변경 금지.
- ⛔ 재작성 횟수를 늘려 품질을 사는 방향으로 가지 마라(비용). 어제 1편이 이미 961초·LLM 6회다.
