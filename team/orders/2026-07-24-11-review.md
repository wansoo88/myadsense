---
id: 2026-07-24-11-review
to: review
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
**네 도구가 정상 길이의 글을 구조적으로 통과 불가로 만들고 있다는 주장이 제기됐다.** 검증하고, 사실이면 고쳐라.
CONTENT 는 생성자이므로 이 주장에 이해상충이 있다. 그래서 **네가 직접 검증**한다. 다만 PM 이 코드로 1차 확인한 결과는 아래와 같다.

## 근거 (PM 1차 확인 — 코드 실물)
- `engine/content/reviewer.py:39` → `return "\n\n".join(parts)[:12000]` — **12,000자 하드 절단 실재**.
- 같은 파일 30행 `strip()` → `re.sub(r"<[^>]+>", " ", ...)` — 태그를 **공백**으로만 치환. `</li>`·`</p>` 경계가 사라진다. `html.unescape()` 없음.
- CONTENT 실측: 기존 발행글 본문 12,341 / 13,081 / 15,168 / 12,985 / 13,258자 — **전부 상한 초과**.
- 결과: 오늘 초안 4건이 전부 `"THE ARTICLE IS TRUNCATED"` 류 **HIGH 자동반려**. 재생성 3회 소진 → 다음 시드 → 무한 반복.

## 작업
- [ ] **① 검증**: 위 3가지(절단·리스트 붕괴·엔티티 누락)가 실제로 오탐을 만드는지 네가 재현하라. `dist/review/*.json` 4건의 반려 사유 원문을 근거로 대조.
- [ ] **② 수정** — 검수기가 **보는 화면**을 고친다:
      - 절단 상한을 실제 본문 길이 이상으로 상향(예: 40,000자). 그래도 자를 일이 생기면 **`[... truncated for review]` 표식**을 붙여 검수기가 절단을 글의 결함으로 오인하지 않게 할 것.
      - 블록 경계(`</li>`·`</p>`·`</h2>` 등)를 **개행**으로 치환.
      - `html.unescape()` 적용.
- [ ] **③ 재검수**: 수정된 검수기로 `dist/review/` 4건을 **다시 판정**하고, 오탐이 사라진 뒤 **어떤 지적이 살아남는지** 보고하라. 살아남은 지적이 진짜 결함이다.
- [ ] **④ 회고**: 기존 28편은 이 상한 아래에서 어떻게 통과했는지 짧게 확인(검수 도입 시점·글 길이 증가 등). 게이트가 언제부터 실질적으로 막고 있었는지 알아야 한다.

## 산출물
- `engine/content/reviewer.py`(네 소유) 수정
- `team/reports/2026-07-24-11-review.md` — 검증·수정·재검수 결과 + 살아남은 진짜 지적 목록

## 완료 기준 (DoD)
- 세 가지 오탐이 **수정 전/후 출력 대조**로 제시돼 있다.
- 4건 재검수 결과가 **항목별로** 나와 있고, **오탐과 진짜 지적이 분리**돼 있다.
- 게이트가 **약해지지 않았음**을 근거로 설명한다.

## 경계 (중요)
- ⛔ **게이트 완화 금지.** CONTENT 가 권고한 3번("미완결·절단 판정을 HIGH 자동반려에서 제외")은 **채택하지 마라.** 그것은 판정 규칙을 무르게 하는 것이다.
      고칠 대상은 **검수기에 들어가는 입력**이지 **판정 기준**이 아니다. 진짜로 잘린 글은 계속 HIGH 로 반려돼야 한다.
- ⛔ 임계값·루브릭·통과 기준을 낮추지 마라. 이번 수정으로 통과율이 오르는 것은 **오탐 제거의 결과여야지 기준 완화의 결과여선 안 된다.**
- ⛔ 콘텐츠 생성·수정 금지. 살아남은 지적은 사유만 제시하고 CONTENT 가 고친다.
- ⛔ 발행·배포 실행 금지.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-11-review | 오탐 3건 <확인|반증> | 재검수 4건 결과 | 살아남은 진짜 지적: ... | 게이트 완화 없음"
```
