---
id: 2026-07-25-30-content
to: content
rev: 1
gate: review-required
status: dispatched
---

## 목적 (왜)
**색인 정면돌파 파일럿.** Google 이 이미 크롤했지만 "다 똑같은 템플릿"이라 색인을 보류한 글을,
새 구조(26-content)로 **재생성**해 재평가를 유도한다. 단 **10편 전량이 아니라 2~3편 파일럿**으로 증거부터 얻는다
(사람 결정 2026-07-25: 색인은 가설이지 보장이 아니므로 손실을 묶고 확대 판단).

## 근거
- 라이브 GSC: indexed **0** / crawled-not-indexed **10** / unknown 27. 5편 전부 기술적으로 색인 가능함은 25-growth 가 실측(200·noindex없음·self-canonical·sitemap포함) → **원인은 기술이 아니라 콘텐츠 차별화**.
- 26-content: 새 구조는 **신규 생성 글부터만** 적용된다(`--stage build` 는 본문 재렌더 안 함) → 기존 글은 **재생성해야** 바뀐다.
- ⚠️ 현재 **특정 slug 재생성 도구가 없다**(`--stage generate` 는 백로그에서 뽑고 발행분은 스킵, `--rereview` 는 검수만).

## 작업
- [ ] **① 대상 선정(3편)**: `crawled-not-indexed` + **유입 0** 인 글만.
      ⛔ 제외: 홈·허브, 그리고 **유입 있는 글**(top_pages: the best ai code review tools 41PV · orca vs herdr 25 · best self hosted alternatives to notion 24 · railway vs render 9).
      25-growth 후보(fly-io-vs-railway / planetscale-vs-neon / claude-code-vs-cursor / cursor-vs-github-copilot)에서 고르되 **정확한 slug 를 sitemap·dist/queue 로 확정**하라.
- [ ] **② 백업 필수**: 대상 3편의 현재 `dist/queue/<slug>.html` + 라이브 HTML 을 타임스탬프 경로에 보존. **경로를 보고**하라. 되돌릴 수 없으면 시작하지 마라.
- [ ] **③ 재생성 수단 마련**: 특정 slug 를 **같은 URL 로** 재생성하는 경로를 만들어라(스크립트 또는 orchestrator 옵션).
      🔴 **URL(slug)은 절대 바뀌면 안 된다** — 바뀌면 기존 크롤 이력이 날아가고 404 가 생겨 색인이 더 나빠진다.
      기존 발행분 스킵 로직을 우회하되, **일반 파이프라인의 품질 게이트·검수는 그대로 통과**해야 한다(게이트 우회 금지).
- [ ] **④ 3편 재생성 + 검수**: 새 구조가 실제로 적용됐는지 확인(유형별 섹션 순서·picklist 등).
      검수 반려면 재작업하되 **2회 시도해도 안 되면 멈추고 보고**(무한 재작성 금지).
- [ ] **⑤ before/after 비교 제시**: 구조 차이(H2 순서 등)·본문 품질·검수 판정·slug 동일성·내부링크/schema 보존.

## 산출물
- 재생성 도구(스크립트 경로), 백업 경로, 재생성된 3편(**큐까지만**), `team/reports/2026-07-25-30-content.md`

## DoD
- 3편이 **같은 slug** 로 재생성되고 **검수 통과**한다(판정 JSON 제시).
- 새 구조가 실제로 반영됐음이 before/after H2 순서 대조로 증명된다.
- 백업이 존재해 **되돌릴 수 있음**이 증명된다.
- 유입 있는 글은 **건드리지 않았음**이 확인된다.

## 경계
- ⛔ **라이브 배포 금지.** 재생성·검수·큐까지만. 라이브 교체는 REVIEW pass + 사람 승인 + OPS.
- ⛔ 품질 게이트·검수 우회 금지(스킵 로직만 우회, 판정은 정상 통과해야 함).
- ⛔ slug·URL 변경 금지. 내부링크·schema·바이라인 손상 금지.
- ⛔ `reviewer.py`·`orchestrator.py` 판정 로직 수정 금지. 키워드 스터핑 등 색인 조작 금지.
