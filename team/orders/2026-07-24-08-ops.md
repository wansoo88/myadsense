---
id: 2026-07-24-08-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
사람이 서버에서 구독 로그인(A안)을 방금 완료했다. **정말로 인증이 살아났는지 실측으로 확인**한다.
"로그인했다"는 말이 아니라 **rc=0 응답**이 근거가 되어야 한다 — 21일간 아무도 모른 이유가 정확히 그 확인의 부재였다.

## 근거
- 사람 보고(2026-07-24): "ssh claude 로그인완료"
- `team/reports/2026-07-24-03-ops.md` — 이전 판정 ③ 미인증(rc=1, `Not logged in`)

## 작업
- [ ] **① 자격증명 상태 재확인**(값 미열람, 존재·mtime·키 이름만):
      `/root/.claude.json` 에 **`oauthAccount` 키가 생겼는지**, `/root/.claude/.credentials.json` 이 **생성됐는지**. 03 보고의 "없음" 과 대조해 변화를 명시.
- [ ] **② 최소 프롬프트 재현** — 03 에서 썼던 것과 동일한 최소 호출 1회.
      **rc=0 이고 `is_error:false` 이며 `duration_api_ms > 0`** 인지 확인(= API 에 실제 도달). 토큰 소모가 최소가 되도록 프롬프트는 한 줄로.
- [ ] **③ 판정**: 인증 살아남 / 여전히 미인증 / 다른 오류(쿼터·모델권한 등) 중 하나로 단정 + 출력 인용.
- [ ] **④ 다음 자동 실행 시점 확인**: `crontab` 상 다음 `daily.sh` 실행이 언제인지(09:00 KST) 명시하고, 그때 생성 편수를 확인할 방법(로그 경로·확인 명령)을 보고에 남길 것.

## 산출물
- `team/reports/2026-07-24-08-ops.md` — 변화 대조표 · 재현 출력 · 판정 · 다음 확인 시점

## 완료 기준 (DoD)
- 03 보고 대비 **무엇이 바뀌었는지**(`oauthAccount` 유무, credentials 파일 유무)가 표로 대조돼 있다.
- 재현 호출의 **rc 와 `duration_api_ms`** 가 인용돼 있다.
- "서버 자율 생성 = 가능 / 불가" 가 단정돼 있다.

## 경계
- ⛔ **`--stage generate` 전체 실행 금지.** 오늘 로컬에서 이미 1편 생성 중이라 일일 카덴스(`daily_generate: 1`)와 충돌한다. 비파괴 경로(`engine/dryrun.py` 등)가 있으면 그것만, 없으면 **최소 프롬프트 재현까지만**.
- ⛔ **서버에서 publish·deploy 실행 금지.** 발행은 REVIEW pass + 사람 승인 후에만(G1).
- 🔒 자격증명 값 출력·복사 금지. 재로그인·키 주입·crontab 변경 금지.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-08-ops | 판정: 인증 <살아남|실패> | rc=? duration_api_ms=? | 다음 cron: 09:00 KST"
```
