---
id: 2026-07-25-29-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
**코드 배포를 완전 자동화한다.** 지금은 사람이 scp 로 밀어야 서버에 반영된다 — 이 수동 단계가 오늘 실제로 사고를 냈다
(CRLF 혼입 → `bash\r` 셰방 깨짐 → 09:00 cron 미실행). 자동화가 곧 재발방지다.

## 근거 (실측 2026-07-25)
- repo `https://github.com/wansoo88/myadsense.git` = **public** (미인증 API 200) → 서버가 **자격증명 없이 pull 가능**.
- 서버: git 2.34.1 설치됨, `git ls-remote` 로 HEAD `67e934e` 확인 = GitHub 도달 OK. 단 `/root/pjt12-adsense` 는 **git repo 아님**(scp 동기화).
- 서버 `renderer.py` 는 **구버전**(md5 로컬 `a5e0d578` vs 서버 `142f9c80`, `_ORDERS`/`picklist` 0건) → 지금 상태면 내일 09:00 새 글도 구 골격.

## 작업
- [ ] **① `.gitattributes` 신설**: 최소 `*.sh text eol=lf` (가능하면 `* text=auto eol=lf`).
      → 체크아웃이 항상 LF 라 CRLF 사고가 **구조적으로** 재발 불가. 로컬 작업트리 영향도 함께 보고.
- [ ] **② 서버를 git 작업트리로 전환**(파괴적일 수 있으니 신중히):
      - **먼저 전체 백업**(`/root/pjt12-adsense` → tar, 경로 보고).
      - 서버에만 있고 repo 에 없는 **런타임 파일이 보존되는지 반드시 확인**: `.env`, `engine/store/*.json|*.db`, `dist/`, 로그.
        (전부 gitignore 대상이어야 한다. 하나라도 추적 대상이면 **중단하고 보고**.)
      - `git init` + remote 추가 + fetch + `main` 체크아웃. **서버에만 있던 수정이 있으면 덮어쓰기 전에 diff 를 보고**하라.
- [ ] **③ `daily.sh` 앞단에 코드 갱신 단계 추가**: `git pull --ff-only` (실패해도 파이프라인은 계속 = 비치명적, 단 **시끄럽게** 로깅).
      ⚠️ pull 은 **selftest 앞**에 두어라 — 새 코드로 회귀 테스트가 돌아야 의미가 있다.
- [ ] **④ 검증**: 서버에서 pull 후 `renderer.py` md5 가 로컬(`a5e0d578b27046594b0ce75afdee89d3`)과 일치, `daily.sh` **CR 0개**, `bash -n` 통과.

## 산출물
- `.gitattributes`, `scripts/daily.sh`, `team/reports/2026-07-25-29-ops.md`(백업 경로·보존 확인·md5 대조 포함)

## DoD
- 서버가 `git pull` 로 스스로 최신 코드를 받는다(실행 로그로 증명).
- `renderer.py` md5 로컬=서버 일치. `daily.sh` CR 0.
- `.env`·`engine/store` 런타임·`dist/` 가 **전환 후에도 그대로** 있음이 파일 목록으로 증명된다.
- pull 실패 시 파이프라인이 죽지 않고 경고를 남긴다(의도적 실패 주입으로 확인).

## 경계
- ⛔ **`.env`·시크릿을 커밋하거나 로그에 출력하지 마라.** repo 가 public 이다 — 실수 시 즉시 노출된다.
- ⛔ 콘텐츠 발행·라이브 교체 금지(이 ORDER 는 코드 배포 자동화까지만). `--stage deploy` 실행 금지.
- ⛔ 킬스위치·cap·판정 임계값 변경 금지. git force push·history 조작 금지.
