---
id: 2026-07-24-01-content
to: content
rev: 1
gate: review-required
status: dispatched
---

## 목적 (왜)
오늘자 일일 카덴스(`guardrails.yaml rollout.daily_generate: 1`)를 채운다. 신규 1편을 **품질 게이트 통과 상태로 큐에 올린다**. 발행은 이 지시의 범위가 아니다.

## 근거
- 사람 요청(2026-07-24): "오늘 글 1편 생성해서 검수까지 돌려라"
- docs/RESEARCH.md F1(품질 우선)·F10(E-E-A-T)·F12(스케일드 콘텐츠 금지 → 페이지별 실질 가치)

## 작업
- [ ] 현재 상태 확인: `dist/research/backlog.json`, `dist/queue/`, `dist/pending_approval/` — 오늘 이미 생성분이 있는지 먼저 본다(중복 생성 방지).
- [ ] 생성 실행. **로컬(로그인된) Claude CLI 경로**로 돌린다 — 서버 생성 경로가 아니다.
      `python engine/orchestrator.py --stage generate`
      (백로그가 비어 있으면 `--stage research` 를 먼저 1회)
- [ ] 신규·니치 툴 주제라 **공식 URL 자동 발견 실패 → 생성 거부**가 나면, 공식 문서 URL 을 직접 주입해 재시도한다(기존 gen_one 주입 방식 확인 후 사용). 주입한 URL 은 보고에 남길 것.
- [ ] 생성물 자체 점검: 고유 가치(데이터·비교·표) 1개 이상 / 근접 중복 없음 / 출처·갱신일·저자 신호 / 허위 1인칭 경험 없음.

## 산출물
- `dist/queue/<slug>/` 또는 `dist/pending_approval/<slug>/`
- 보고서 `team/reports/2026-07-24-01-content.md`

## 완료 기준 (DoD)
- 신규 1편이 품질 게이트를 통과해 큐(또는 휴먼 게이트 대기)에 존재한다.
- 보고에 **슬러그 · 제목 · 타깃 키워드 · 고유 가치가 무엇인지 · 인용 소스 URL** 이 들어 있다.
- 생성이 거부됐다면 **거부 사유와 재현 가능한 로그**를 보고한다(억지 통과 금지).

## 경계
- 발행·배포 실행 금지(`--stage publish`, `deploy.py`, 서버 ssh) — OPS 소관.
- **자기 검수 통과 선언 금지**. 검수는 REVIEW 가 별도 지시로 한다.
- 게이트를 낮추거나 우회하는 코드 수정 금지.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-01-content | 결과 | 산출물 | 이슈"
```
이 작업 보고 후에는 **이어서 2026-07-24-02-content 지시서를 수행**한다(별도 하달됨).
