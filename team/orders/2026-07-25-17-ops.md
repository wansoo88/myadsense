---
id: 2026-07-25-17-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
**우리 사이트의 개인정보처리방침이 사실과 다르다.** 승인 심사 리스크다(F2 는 강제 필수 페이지).
그리고 어제 만든 회귀 테스트가 **아무도 부르지 않아** 구멍 재개방을 못 잡는다.

## 근거 (12-review rev2 감사, 2026-07-24)
- `engine/content/site_builder.py:216` 이 `/privacy/` 에 **"We use Google AdSense to display ads"** 를 현재형 단정으로 서술.
  같은 시점 관측기 판정: 렌더 템플릿 2개 + 빌드 39페이지 **전수에서 광고 코드 0건**. `:222-223`·`:260-263` 도 동일 성격.
- `engine/content/reviewer_selftest.py`(245줄, 28체크) 가 `scripts/daily.sh`·`config/schedule.yaml`·CI 어디에서도 **호출되지 않는다**(grep 0건).

## 작업
- [ ] **① `/privacy/` 문구를 실제 상태와 일치시켜라.** ⛔ 고지를 **삭제하지 마라** — 광고 게재 시 반드시 있어야 한다(F2).
      "현재 게재 중"과 "게재하게 되면"을 구분하는 표현으로 바꾸거나, `reviewer.monetization_state()` 와 같은
      **관측 값에 연동**해 자동으로 현재형/조건형이 바뀌게 하라. 방식은 네가 판단하되 **근거는 관측값**이어야 한다.
- [ ] **② `reviewer_selftest.py` 를 파이프라인에 배선.** `scripts/daily.sh` 또는 스케줄에서 호출하고,
      **실패 시 눈에 띄게** 하라(조용한 성공보다 시끄러운 실패). ⚠️ 단, 셀프테스트 실패가 **발행을 막는 게** 맞는지는
      네 판단으로 제안하고 PM 승인을 받아라(테스트 깨짐 = 발행 중단은 과할 수 있다).
- [ ] **③ 20:00 로컬 배치 소요시간 관측.** 초안당 재작성 1회가 상시화되면(발행 28편 중 25편이 플래그 발화)
      생성 호출/시간이 최대 2배다. 배치가 시간 안에 끝나는지 **실측 근거**를 남겨라.

## DoD
- `/privacy/` 렌더 결과가 **광고 0건 상태에서 사실인 문장**임을 빌드 산출물로 제시. 광고가 생기면 문구가 바뀌는지도 확인.
- 셀프테스트가 실제로 호출되는 것을 **실행 로그로** 증명(28체크 결과 포함).
- 배치 소요시간 실측치(분) 제시.

## 경계
- ⛔ 발행·배포 실행은 REVIEW pass + 사람 승인 후에만. 이 ORDER 로 라이브에 밀지 마라.
- ⛔ `reviewer.py`·`generator.py` 수정 금지(타 소유). 관측 함수는 **호출만** 하라.
- ⛔ 킬스위치·`daily_publish_cap` 등 가드레일 값 변경 금지.
