---
id: 2026-07-24-09-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
`/favicon.ico` 가 **404 × 38회**(Googlebot-Image 포함) 나고 있다. 크롤 예산이 이미 극도로 부족한(Googlebot 15일간 16회) 사이트에서 없는 파일을 반복 요청하게 두는 것은 낭비이고, 완성도 신호에도 손해다.
색인 문제의 주원인은 아니지만(1순위는 크롤 예산), **비용 대비 확실히 정리되는 항목**이다.

## 근거
- `team/reports/2026-07-24-04-growth.md` §2 부수 발견 · §5 OPS 조치안 1번
- docs/RESEARCH.md F13(페이지 경험 — 폭넓은 완성도 신호)

## 작업
- [ ] favicon 을 **빌드 산출물에 포함**시킨다. ⚠️ 서버 cron 이 매일 `/root` 소스로 재빌드하므로 **web_root 에 수동 업로드하면 다음 빌드에서 지워진다** — 반드시 빌드 경로(`engine/content/site_builder.py`)에서 생성·복사되게 할 것.
- [ ] 디자인 일관성: `design/design.md` 의 색/톤을 따르는 **단순한 도형/이니셜** 수준이면 충분하다. 외부 에셋·폰트 다운로드 금지(자립적으로).
- [ ] `/favicon.ico` 가 **200** 으로 응답하는지 배포 후 **https** 로 확인(http 는 301만 나온다).
- [ ] 부수: 로그에서 404 가 잦은 나머지 경로(`/atom.xml`·`/sitemap_index.xml` 등)는 **봇 추측 요청이므로 대응 불필요** — 만들지 말 것(불필요한 URL 증식은 오히려 해롭다). 확인만 하고 넘어간다.

## 산출물
- `engine/content/site_builder.py`(또는 빌드 자산 경로) + favicon 자산
- `team/reports/2026-07-24-09-ops.md`

## 완료 기준 (DoD)
- 로컬 빌드 산출물에 favicon 이 포함된다.
- 배포 후 `https://stack.utilverse.info/favicon.ico` 가 **200** 응답(실제 응답 코드 인용).
- 재빌드해도 유지된다(빌드 경로에서 생성됨을 근거로 설명).

## 경계
- 콘텐츠·템플릿 본문 로직 변경 금지(`renderer.py` 는 CONTENT 소관). **`<link rel="icon">` 태그 추가가 꼭 필요하면 하지 말고 PM 에 보고하라** — `/favicon.ico` 관례 경로만으로 충분하다.
- 배포는 기존 원자적 교체 경로 그대로. nginx 설정·crontab 변경 금지.
- ⛔ 크롤 유도를 위한 어떤 요청 발생·핑 반복도 금지(G4).

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-09-ops | favicon 200 확인 | 빌드 포함 근거 | 이슈"
```
