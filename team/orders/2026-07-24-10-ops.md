---
id: 2026-07-24-10-ops
to: ops
rev: 1
gate: none
status: dispatched
priority: 긴급 — 2026-07-25 09:00 KST 이전
---

## 목적 (왜)
**내일 09:00 서버 cron 이 인증은 통과하고도 같은 자리에서 또 죽는 것을 막는다.**
CONTENT 가 3일간의 로컬 침묵 실패 원인을 `stdin` 상속으로 규명하고 `generator.py` 에 `stdin=subprocess.DEVNULL` 을 넣었다(01 보고 §2). 그런데 **네가 07 에서 서버에 올린 generator.py 는 그 수정 이전 버전**이다.
서버 cron 도 콘솔·tty 가 없는 환경이라 **동일 증상이 재현될 가능성이 높다.** 인증만 고쳐놓고 내일 또 0편이면 오늘 한 일이 무의미해진다.

## 근거
- `team/reports/2026-07-24-01-content.md` §2 — `rc=1` + stderr 공백 = 콘솔/stdin 없는 환경의 전형적 증상, DEVNULL 로 rc=0(16.6초) 정상 복귀
- `team/reports/2026-07-24-08-ops.md` — 다음 자동 실행 2026-07-25 09:00 KST
- PM 확인: 로컬 `generator.py` 에 `_cli_error_detail`(네 것, 185행)과 `stdin=DEVNULL`(CONTENT, 269·446행)이 **충돌 없이 공존**한다.

## 작업
- [ ] 로컬 `engine/content/generator.py` 와 **서버 배포본을 대조**해 차이를 확인(네 로깅 수정이 살아 있는지, stdin 수정이 빠져 있는지).
- [ ] 최신 로컬본을 서버로 동기화. ⚠️ 서버 cron 이 `/root` 소스로 재빌드하므로 **엔진 소스 경로**에 넣어야 한다(dist 만 올리면 무효).
- [ ] **서버에서 비대화형(콘솔 없는) 컨텍스트로 재현 검증**: cron 과 동일한 조건에 가깝게(예: `ssh ... 'bash -lc "... < /dev/null"'` 등 tty 없는 호출) 최소 프롬프트 1회 → **rc=0** 확인.
      → 이것이 이 지시의 핵심이다. 대화형 ssh 에서 성공하는 것은 이미 08 에서 확인했고, **증명해야 할 것은 tty 없는 경로**다.
- [ ] 그래도 실패하면 사유를 로그에서 확보해(07 의 로깅 수정 덕에 이제 사유가 찍힌다) 보고. **임의로 crontab·인증을 만지지 말 것.**

## 산출물
- 서버 `engine/content/generator.py` 동기화
- `team/reports/2026-07-24-10-ops.md` — 대조 결과 · tty 없는 재현 검증 로그 · 내일 합격 기준

## 완료 기준 (DoD)
- 서버 배포본에 `stdin=DEVNULL` **과** `_cli_error_detail` 이 **둘 다** 존재함이 확인됐다.
- **tty 없는 호출에서 rc=0** 이 실측 인용돼 있다(이게 없으면 DoD 미충족).
- 내일 09:00 결과를 판정할 기준·확인 명령이 보고에 있다.

## 경계
- `generator.py` 는 CONTENT 소유다. **동기화(복사)만 하고 내용을 새로 고치지 말 것.** 추가 수정이 필요하다고 판단되면 PM 에 보고.
- crontab·인증·nginx 무변경. publish·deploy 실행 금지.
- 🔒 자격증명 값 미출력.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-10-ops | 동기화 완료 | tty없는 호출 rc=? | 내일 09:00 합격기준"
```
