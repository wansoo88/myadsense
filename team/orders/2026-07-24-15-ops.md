---
id: 2026-07-24-15-ops
to: ops
rev: 1
gate: none
status: 대기(herdr API 복구 후 하달)
---

## 목적 (왜 하는가)
오늘 하루를 통째로 태운 논쟁("반려가 글 탓인가 도구 탓인가")이 **3분이면 끝날 수 있었다** — 반려된 초안이
남아 있었다면. 지금 `orchestrator.py` 는 **통과분만** 저장하고 반려 초안은 버린다. 사후 검증이 구조적으로 불가능하다.

## 근거
- `team/reports/2026-07-24-11-review.md` ③ 도입부·이슈 — "반려된 초안은 어디에도 보존되지 않는다
  (`orchestrator.py:157` 판정 JSON 만 저장, 통과분만 `dist/queue`)" → **오늘 4건 원본 소실, 원문 재검수 불가**.
- REVIEW 가 `reviewer.py` 쪽 입력 로그(`dist/review/<slug>.input.txt`)는 이미 만들었으나,
  **초안 spec 자체 보존은 `orchestrator.py`(OPS 소유)** 라 손대지 않고 넘겼다.

## 작업
- [ ] 반려 시 초안 spec 을 `dist/review/<slug>.spec.json` 으로 보존(판정 JSON 과 **같은 slug 로 짝**이 맞게).
- [ ] 보존 실패가 **파이프라인을 멈추지 않게** 한다(try/except — 부가 기능이 본류를 죽이면 안 된다).
- [ ] `dist/` 는 gitignore·배포는 `dist/site` 만 → **리포·서버·배포에 영향 없음**을 확인해 보고.
- [ ] 보존된 spec 으로 **재검수를 다시 돌릴 수 있는지** 실제로 1회 증명(같은 slug 로 reviewer 재실행).

## 산출물 (정확한 경로)
- `engine/orchestrator.py`
- `dist/review/<slug>.spec.json` (실제 생성된 예시 1건 경로 제시)
- `team/reports/2026-07-24-15-ops.md`

## 완료 기준 (DoD — 검증 가능하게)
- 반려 1건에서 spec.json 이 실제로 생성됨을 파일 경로·크기로 제시.
- 그 파일만으로 **재검수 재현 1회 성공**(입력 로그와 대조).
- 보존 로직이 예외를 던져도 파이프라인이 계속 도는 것을 **의도적 실패 주입**으로 확인.

## 경계 (하지 말 것)
- ⛔ 발행·배포 실행 금지(REVIEW pass + 사람 승인 없이는 어떤 발행도 없다).
- ⛔ 검수 판정 로직·임계값 수정 금지(REVIEW 소유).
- ⛔ 저장 대상에 **API 키·토큰·`.env` 값이 섞이지 않게** 할 것(spec 만 저장).
- ⛔ `dist/site` 구조 변경 금지(배포 산출물).

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-15-ops | spec 보존 경로 | 재검수 재현 결과 | 실패주입 확인"
```
