---
id: 2026-07-25-22-content
to: content
rev: 1
gate: review-required
status: 대기(사람의 인증 결정 후 하달)
---

## 목적 (왜)
검수 반려 사유가 5종에서 **새 유형**으로 옮겨갔다. 그리고 실패 사유를 **읽을 수 없게 잘라먹는 버그**가 있다.

## 근거 (2026-07-25 재생성·감사 실측)
- 신규 초안 `proton-pass-vs-bitwarden…`: `severity=medium`, ai_tells 4건. 남은 지적의 성격이 바뀌었다 —
  **확인한 근거 범위를 넘는 단정**: verdict `"the only one of the two whose pages describe…"`(확인한 건 홈·가격 2페이지인데
  'whose pages' 가 전체 문서를 덮는 것처럼 읽힘) · `"will show you what survives in under an hour"`(핸즈온을 부인하는 글에서
  소요시간 단정) · `"identifies the leaking party immediately"`(별칭은 주소를 받은 사이트를 지목할 뿐 유출 주체 단정 불가).
- `generator.py:351 _cli_error_detail` 이 `stderr + result` 를 붙여 `[:300]` 한다 → stderr 에 314자 워크스페이스 신뢰 경고가
  있으면 **진짜 사유가 완전히 잘려나간다**(실측: `'Not logged in'` 포함 False). 예약 배치 로그의 "사유 공백"이 여기서 나왔다.
- 생성기 자가검수 정규식 17개의 **도달률 9%**(검수기 ai_tells 인용문 266개 중 23개). 두 시도 모두 자가검수 0건인데 검수기는 7건·4건.
- 날짜 도장 반복(`(checked 25 July 2026)` 약 12회)은 `_user_prompt` 의 OBSERVATION DATE 지시가 유발한 **자책골**.
  발행 31편 평균 0.1회 vs 신규 초안 11~23개.

## 작업
- [ ] **① `_cli_error_detail` 절단 수정.** 진짜 사유(`result` 의 에러 메시지)가 **먼저·온전히** 보이게 하라.
      stderr 의 상용 경고는 뒤로 밀거나 걸러라. 실패 사유를 못 읽으면 모든 진단이 추측이 된다.
- [ ] **② 근거 범위 초과 단정 차단.** "확인한 페이지"와 "제품 전체"를 구분하도록 프롬프트에 규칙을 넣어라
      (예: 소스가 N개 페이지면 주장 범위도 그 페이지로 한정하는 표현을 쓰게). 5종에 ⑥으로 추가.
- [ ] **③ 날짜 도장 입도.** OBSERVATION DATE 지시를 **문단마다**가 아니라 **섹션 첫 등장 1회**로 좁혀라.
      ⚠️ 이건 ③(무헤지 수치)과 상충할 수 있다 → **REVIEW 와 조율**하고 자가검수 판정 입도를 임의로 바꾸지 마라. 제안을 PM 에 올려라.
- [ ] **④ 자가검수 도달률 9% 개선 여부 판단.** 정규식을 늘려 쫓아가는 게 맞는지, 아니면 자가검수는 **저비용 1차 필터**로
      한정하고 검수기 피드백(20-ops 가 이미 전달 경로를 열었다)에 맡기는 게 맞는지 **근거를 들어 권고**하라. 늘리기가 목적이 아니다.

## DoD
- ① 실패 사유가 온전히 로그에 남는 것을 **실제 실패를 유발해** 증명(예: 빈 config 디렉터리로 `Not logged in`).
- ② 근거 범위 초과 문장을 담은 픽스처가 **플래그된다**. 정상 범위 문장은 플래그되지 않는다(양방향).
- ③ 날짜 도장 빈도 전/후 실측 비교. ④ 권고에 수치 근거가 있다.

## 경계
- ⛔ 발행·배포 금지. ⛔ `reviewer.py`·`orchestrator.py` 수정 금지(타 소유).
- ⛔ 검수 상한·판정 임계값 되돌리기 금지. ⛔ 재작성 횟수 늘려 품질 사기 금지(1편 961초·LLM 6회가 현재값).
