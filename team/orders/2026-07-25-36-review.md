---
id: 2026-07-25-36-review
to: review
rev: 1
gate: none
status: dispatched (2026-07-25)
---

## 목적 (왜)

오늘 워커가 전원 다운된 사이, **PM 이 REVIEW·CONTENT 소유 파일을 직접 수정**했다(ORDER 21 전건 + 22 ①).
사람 승인으로 이미 **push 되어 서버가 그 코드로 돈다**(커밋 `7492102`, 서버 HEAD 반영·selftest ALL PASS 확인).

권한분리 원칙상 **만든 자가 자기 코드를 검수한 상태**다. 그 형해화를 사후에라도 메우는 것이 이 ORDER 다.
⛔ 눈치 보지 마라. 결함을 찾으면 그대로 올려라 — PM 이 짠 코드라는 사실은 판단에 영향을 주면 안 된다.

## 검수 대상 (커밋 `7492102`)

- `engine/content/reviewer.py` — `_coerce_passed` 신설 + `data["passed"]` 정규화, 고지 표식 `move`→`copy`,
  `_AD_CODE_RE` 한계 주석
- `engine/content/generator.py` — `_cli_error_detail` 항목별 상한 분리(`_ERR_RESULT_CHARS`/`_ERR_STDERR_CHARS`)
- `engine/content/reviewer_selftest.py` — 신규 `[5][6][7]` 12항목
- `.claude/skills/adsense-audit/SKILL.md` — "선언이 1차·탐지는 백스톱"

## 작업

- [ ] **① `_coerce_passed` 를 뚫어봐라.** 검수기가 낼 법한 값으로 **통과가 뒤집히는 입력**을 찾아라.
      화이트리스트(`true/yes/y/t/1/pass/passed/ok`)가 과하게 넓지는 않은가? 반대로 **정상 통과를 막는**
      오탐은 없는가(예: LLM 이 `"True"`·`"YES"`·`true` 를 낼 때). 양방향으로 쳐라.
- [ ] **② ORDER 19 불변식이 살아 있는지 재확인.** "어떤 입력에도 `passed` 가 True 로 바뀌지 않는다" —
      `_coerce_passed` 가 이 불변식을 **약화시키지 않았는지** 독립적으로 검증하라(selftest 를 믿지 말고 직접).
- [ ] **③ `copy` 전환의 부작용을 찾아라.** `issues` 에 지적이 남으면서 **중복 계산되는 경로**는 없는가?
      (예: 보고서 집계·재작성 피드백 길이·`review_feedback` 의 `max_issues` 6 상한에 밀려 다른 지적이
      잘려나가지 않는가.) 실코퍼스로 확인하라.
- [ ] **④ `_cli_error_detail` 의 토큰·시크릿 노출 위험.** result 상한을 800자로 키웠다.
      CLI 오류 본문에 **경로·환경변수·토큰이 섞여 로그에 남을 가능성**을 점검하라.
- [ ] **⑤ 신규 테스트가 진짜를 재는지.** `[6]` 은 `complete_text` 를 몽키패치한다 —
      **실제 경로를 우회한 가짜 증명은 아닌가?** 테스트가 실패해야 할 때 실패하는지 역으로 확인하라
      (예: `_coerce_passed` 를 일부러 망가뜨리면 `[5][6]` 이 붉게 뜨는가).

## DoD

- ①~⑤ 각각 **판정 + 근거(실행 결과)**. "문제 없음"도 근거가 있어야 한다.
- 결함 발견 시 **재현 절차**와 심각도. 발행 게이트에 영향이면 즉시 `tell.py pm` 으로 먼저 알려라.
- 수정 제안은 **제안까지만** — 타 소유 파일이므로 직접 고치지 마라(발견자≠수정자).

## 경계

- ⛔ 파일 수정 금지(검수·보고만). ⛔ 발행·배포·push 금지.
- ⛔ `passed` 판정을 정규식으로 뒤집는 기전을 **되살리자고 제안하지 마라** — 세 라운드 실패로 폐기됐다(ORDER 19).
