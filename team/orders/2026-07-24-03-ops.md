---
id: 2026-07-24-03-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
서버(115.68.230.40)의 Claude CLI 가 **사람의 구독 계정으로 정상 인증되어 cron 생성 단계가 실제로 돌고 있는지** 확인한다.
기록상으로는 "서버 CLI 는 로그인 안 됨 → 글 생성은 로컬에서, 서버 cron 0편은 정상"이었으나, 사람이 "서버에서 구독 ID로 돌게 개발돼 있다"고 한다. **둘 중 무엇이 현재 사실인지 데이터로 판정**한다.

## 근거
- 사람 요청(2026-07-24): "서버에서 지금 claude가 내 구독id로 돌겠금 개발이 되어있는데 이상없는지 확인"
- AUTOMATION.md §5(서버 cron 1차 구동) · CLAUDE.md(배포·서버 정보)

## 작업
- [ ] 서버 접속: `ssh -i ~/.ssh/autobtc_iwinv root@115.68.230.40`
- [ ] Claude CLI 상태: 설치 경로·버전·인증 방식(구독 OAuth 로그인 vs `ANTHROPIC_API_KEY`) 판별. `claude --version`, 인증 상태 확인 명령, 관련 설정/자격증명 **파일의 존재·수정시각만** 확인.
- [ ] cron 실측: `crontab -l` 과 `scripts/daily.sh` 에서 **생성 단계가 어떤 명령으로 호출되는지** 추적.
- [ ] **최근 7일 로그**에서 생성 결과 확인: 하루 몇 편 생성됐는지, 실패 시 에러 메시지 원문(인증 실패 / 쿼터 / 인코딩 / 그라운딩 거부 등 구분).
- [ ] 판정: ① 구독 인증으로 정상 동작 ② API 키로 동작(구독 아님) ③ 미인증·0편 — 셋 중 하나로 결론 + 근거 로그 인용.
- [ ] 이상이 있으면 **조치안만** 제시(원인 → 필요한 조치 → 사람 승인이 필요한 항목 구분).

## 산출물
- `team/reports/2026-07-24-03-ops.md` — 판정·근거 로그·조치안

## 완료 기준 (DoD)
- "서버 Claude = 정상 / 비정상" 이 **로그 인용과 함께** 단정돼 있다.
- 최근 7일 서버측 생성 편수가 숫자로 제시돼 있다.
- 비정상이면 원인이 인증·쿼터·스크립트·인코딩 중 무엇인지 구분돼 있다.

## 경계
- 🔒 **자격증명 값(토큰·API 키·세션)을 절대 출력·복사·전송하지 않는다.** 존재 여부·수정시각·마스킹된 앞 4자만.
- 재로그인·재인증·키 교체·CLI 재설치·crontab 변경은 **하지 않는다**(사람 승인 사항). 이번엔 **읽기·진단만**.
- 파괴적 명령(rm -rf, 서비스 재시작, nginx 설정 변경) 금지.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-03-ops | 판정: ... | 최근7일 생성 N편 | 조치필요: ..."
```
