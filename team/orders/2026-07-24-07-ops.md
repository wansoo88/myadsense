---
id: 2026-07-24-07-ops
to: ops
rev: 1
gate: none
status: dispatched
---

## 목적 (왜)
**같은 침묵 실패가 재발하지 않게 막는다.** 서버 생성이 21일간 0편이었는데 아무도 몰랐던 이유는 생성 실패가 로그에 사유 없이 남고(`claude CLI 실패(rc=1): `), 전 편 실패해도 단계가 rc=0 을 반환했기 때문이다(2026-07-24-03-ops 이슈 2번).
인증을 어느 쪽으로 풀든 **이 결함이 먼저 고쳐져야** 다음 고장을 당일에 안다.

## 근거
- `team/reports/2026-07-24-03-ops.md` 이슈 2번 (OPS 본인 진단)
- 사람 결정(2026-07-24): 인증은 **A안(서버 구독 로그인)** 채택 + 관측성 결함 **즉시 수정** 승인

## 작업
- [ ] **① 실패 사유 로깅**: `engine/content/generator.py` 의 claude CLI 실패 처리(약 426행 부근)에서 `stderr` 뿐 아니라 **stdout 의 JSON `result` 필드도 함께 로그에 남기도록** 수정. CLI 는 오류를 stdout 에 넣기 때문에 지금은 사유가 공백으로 찍힌다.
      → 수정 후 로그가 `claude CLI 실패(rc=1): Not logged in · Please run /login` 형태로 사유를 남겨야 한다.
- [ ] **② 침묵 실패 경보**: "신규 생성 0편이 N일 연속"이면 알림이 가도록 추가. 위치는 `engine/monitor/` 또는 `scripts/daily.sh` 중 **기존 알림 경로를 재사용**하는 쪽으로(새 채널 만들지 말 것). 임계값 N 은 `config/guardrails.yaml` 에 값으로 두고 하드코딩하지 않는다(기본 2일 제안).
      ⚠️ 이 경보는 **알림만** 한다. 킬스위치(발행 중단)와 혼동하지 말 것 — 생성 0편은 정지 사유가 아니다.
- [ ] **③ 검증**: 실제로 실패를 한 번 재현해 로그에 사유가 찍히는지 확인(인증이 아직 안 된 상태라면 지금 그대로 재현된다). 경보 로직은 드라이런으로 확인.

## 산출물
- `engine/content/generator.py`(로깅 1개소), 경보 코드 1개소, `config/guardrails.yaml`(임계값)
- `team/reports/2026-07-24-07-ops.md` — 변경 요약(diff 요지)·검증 로그

## 완료 기준 (DoD)
- 실패 재현 시 로그에 **사유 문자열이 실제로 찍힌다**(검증 로그 인용).
- "0편 N일 연속" 경보가 기존 알림 경로로 나가며, 임계값이 config 값으로 관리된다.
- 기존 정상 경로(생성 성공 시 동작, 킬스위치, 발행 cap)에 **부작용이 없다**.

## 경계
- 🔑 **소유권 예외(PM 승인)**: `engine/content/generator.py` 는 원래 CONTENT 소유다. 이번 건에 한해 **실패 로깅 1개소만** OPS 가 수정한다. 프롬프트·생성 로직·품질 게이트는 **건드리지 않는다**.
- 인증 관련 조치(로그인·키 주입)는 이 지시에 포함되지 않는다 — 사람이 A안을 수행 중이다.
- crontab 변경·서비스 재시작 금지.

## 보고
```powershell
python scripts/tell.py pm "DONE 2026-07-24-07-ops | 로깅·경보 수정 완료 | 검증: <재현 로그> | 변경파일: ..."
```
