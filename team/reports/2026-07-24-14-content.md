---
id: 2026-07-24-14-content
from: content
to: pm
rev: 2
result: done            # rev1 = partial(생성 미실행) / rev2 코드 항목 = done / rev2 생성 항목 = done(실행 완료, 글은 검수 반려)
date: 2026-07-24        # rev2 생성 결과 추가: 2026-07-25 00:05
gen_result: rejected    # 신규 1편 생성·검수 완료 → passed=false(severity=medium). 5종 중 ⑤ 재발·① 부분 재발, 새 사유 2건 + 인프라 결함 1건(WinError 206)
---

# 보고 — 진짜 결함 5종을 생성 프롬프트 레벨에서 차단

## 결과 요약
5종을 **프롬프트(예방) + 생성측 자가검수 스캐너(검출) + 1회 자가 재작성(교정)** 3중으로 막았다. 각 차단 지점은 아래 라인번호·문구 diff로 제시한다.
검증은 **LLM 실호출 0회** — 반려 4건에서 인용된 결함 문장 원문을 스캐너에 그대로 투입해 5종 전부 검출됨을 실측했고, 배선은 `_via_api` 몽키패치로 4개 케이스(개선채택·악화거부·예외내성·킬스위치) 통과.
⚠️ **DoD 2·3(신규 1편 생성·검수결과 제시)은 미충족** — PM 지시로 이번 회차는 "코드·프롬프트 수정까지, 생성 실행 금지". 그래서 `result: partial`. 게이트는 한 줄도 낮추지 않았다(아래 ⑥ 참조).

---

## ① 5종 차단 지점 (요구된 프롬프트 문구 diff)

3중 방어의 역할 분담:
- **예방(프롬프트)** — `_SYSTEM`(모든 글 공통) + `_user_prompt`(토픽·소스 문맥 의존)
- **검출(스캐너)** — `generator.selfcheck()` : 생성 직후 산문을 정규식으로 훑는다. **판정 아님**(반려 권한 없음)
- **교정(재작성)** — 검출되면 사유를 피드백으로 **1회만** 다시 쓰게 한다. 그래도 남으면 그대로 REVIEW 로 보낸다

### ① 최상급 제목 vs 본문 불일치
| 층 | 위치 | 문구 |
|---|---|---|
| 예방(공통) | `generator.py:581` (`_SYSTEM` 신규 불릿) | `- TITLE CONTRACT: the title may promise only what the body delivers. If the title says "best", "cheapest", "top N" or otherwise ranks, the body MUST (a) state the selection criteria explicitly and (b) name a specific pick per use case in tldr_html and verdict_html. If the material does not let you rank honestly, do not hedge inside a superlative title — change the title ("How to choose ...", "... compared"). A title that ranks over a body saying "there is no single best/cheapest" is a misleading headline.` |
| 예방(토픽별) | `generator.py:441-452` (`_user_prompt`, **시드가 최상급일 때만** 주입) | `TITLE CONTRACT (this query asks for a superlative — pick ONE of these two, do not blend them): (a) DELIVER IT: state the selection criteria explicitly … (b) RETITLE: … 'How to choose a cheap VPS in 2026' … The 'title' field is yours to set; it does not have to repeat the query.` |
| 검출 | `generator.py:255-270` + `selfcheck()` `372-385` | `SUPERLATIVE_TITLE_RE` ∧ (`RANK_REFUSAL_PATTERNS` 발견 ∨ `PICK_RE` 부재 ∨ `CRITERIA_RE` 부재) |
| 정책 | `config/topics.yaml:10-21` `title_policy` | `superlative_requires: [stated_selection_criteria, named_pick_per_use_case, opinion_framing]` / `otherwise: retitle` |

`RANK_REFUSAL_PATTERNS` 는 추정이 아니라 **반려문 원문**에서 뽑았다 — `"there is no single cheapest VPS"`, `"no specific figures are quoted here"`, `"takes a different angle from a ranked list"`.

### ② 경쟁사 단정적 부정 주장
| 층 | 위치 | 문구 |
|---|---|---|
| 예방(공통) | `generator.py:582` | `- NEGATIVE CLAIMS about a named product ("X does not support Y", "X lacks Y", "X has no Z", or the same thing implied): only when a cited page positively establishes the absence, and always scoped and dated … Do not describe one vendor from cited pages and its competitor from memory.` |
| 예방(그라운딩 블록) | `generator.py:464-469` | `NEGATIVE CLAIMS: this material is a PARTIAL excerpt of each page. Something missing here is NOT evidence that the vendor lacks it. … scope it to what you checked: "the pricing page listed no managed database as of 2026-07-24". Do the same for both vendors — never describe one from the sources and the other from memory.` |
| 검출 | `generator.py:273-285` + `selfcheck()` `388-393` | 문장 단위로 `ABSENCE_CLAIM_RE` ∧ ¬`SCOPE_CUE_RE` → 플래그 |

**"소스에 안 보임 ≠ 벤더에 없음"** 을 명시한 것이 핵심이다(11-review 오탐 4: 소스 절단 때문에 생긴 착시가 그대로 본문의 단정으로 굳었다).

### ③ 휘발성·제3자 수치 헤지
| 층 | 위치 | 문구 |
|---|---|---|
| 예방(공통) | `generator.py:583` | `- VOLATILE FACTS: every price, plan name, quota, limit, model/region count and benchmark figure in prose carries an as-of date … Third-party benchmark and customer-result stats ("customer X saw 67% lower cost", "2x throughput") are marketing claims — leave them out unless the vendor's own cited page carries them, and then attribute them to that page rather than stating them as measured fact.` |
| 예방(토픽별) | `generator.py:436-440` | `OBSERVATION DATE: 2026-07-24. Every price, plan name, quota, limit, model/region count or benchmark figure you put in prose is an observation made on 2026-07-24 — write that date next to the figures … If you cannot tie a number to a cited page, leave the number out.` (시스템 날짜 주입, 모델이 날짜를 지어내지 않게) |
| 검출 | `generator.py:287-294` + `selfcheck()` `396-401` | 단락 단위 `VOLATILE_FIGURE_RE` ∧ ¬`DATE_CUE_RE` → 플래그 |

ORDER 가 지목한 `Workato 67%` 예시가 프롬프트 문구에 **그대로** 박혀 있다.

### ④ 파이프라인 언어 노출
| 층 | 위치 | 문구 |
|---|---|---|
| 예방(공통) | `generator.py:587` | `- NO PIPELINE LANGUAGE: write for a reader who knows nothing about how this page was produced. Never mention fetching, scraping, excerpts, drafts, "the text reviewed", or supplied/provided source material ("the fetched text", "the supplied sources", "in the version reviewed", "as delivered", "[SOURCE 1]"). … Never promise a scoring or marking system you do not actually output ("marks and descriptions below"). Never point at page positions ("the table below", "listed above", "the sources section at the end") — the site template, not you, decides where the tables, the verdict and the sources land; refer to sections by name.` |
| 예방(그라운딩 블록) | `generator.py:470-473` | `NEVER DESCRIBE THIS MATERIAL IN THE ARTICLE. The reader has no idea it exists. Do not write 'the fetched text', 'the supplied sources', 'the excerpt', 'in the version reviewed', '[SOURCE 1]' …` |
| 검출 | `generator.py:296-316` (`PIPELINE_LEAK_PATTERNS` 13종) + `selfcheck()` `404-409` | ORDER 지정 4문구 전부 포함 |

**위치 지시(below/above)까지 금지어에 넣은 근거**: 표·verdict·sources 의 배치는 모델이 아니라 `renderer.py:452-495` 가 정한다 → 3번 초안의 `"marked as absent below"` 방향 오류는 구조적으로 재발한다. `fetch` 기본형은 제외했다(`git fetch` 오탐 방지) — `fetched|fetching` 만 잡는다.

### ⑤ AI 티
| 층 | 위치 | 문구 |
|---|---|---|
| 예방(공통) | `generator.py:588` (기존 클리셰 불릿을 `- VOICE:` 로 확장) | `Do NOT (a) close sections with an aphoristic one-liner, (b) repeat the "X, not Y" / "not X, but Y" antithesis, (c) open sections with counted lists ("Five things move the real cost...", "Three situations regularly end with..."), (d) comment on the article itself ("this guide takes a different angle", "read those two paragraphs together", "most comparison articles admit"), (e) put an em-dash aside in most paragraphs, or (f) use the word "genuine" or "genuinely".` + 기존 금지구에 `"it's worth noting" (or variants like "worth stating outright")` 추가 |
| 검출 | `generator.py:317-338` (`GEN_STYLE_BANS` 8종 + `MIRROR_MAX=3` + `EM_DASH_PARA_RATIO_MAX=0.60`) | |

**프롬프트 자체가 "genuine" 을 씨 뿌리고 있었다** — 이게 이번 회차의 가장 값싼 발견이다:
```diff
- Write genuinely useful, original content that satisfies search intent. Rules:      (구 _SYSTEM 2행)
+ Write useful, original content that satisfies search intent. Rules:                (신 _SYSTEM:579)
```
검수기가 매번 지적하던 `"genuine(ly)" 남용`은 모델의 습관이기 전에 **우리가 시스템 프롬프트 첫 줄에서 시킨 것**이었다. 발행 28편에서 이 단어가 **13회** 발화한다(아래 ③ 실측). 같은 이유로 내가 새로 쓴 그라운딩 문구에서도 `"If the absence genuinely matters"` → `"If the absence matters to the decision"` 로 뺐다(`generator.py:466`).

---

## ② 배선 — 검출되면 어떻게 되는가 (`generator.py:100-126`)

```python
spec = _one(feedback)
if os.environ.get("ADSENSE_SELFCHECK") != "0":
    flags = selfcheck(spec)
    if flags:
        ...  # 사유를 피드백으로 1회만 재작성
        if len(flags2) <= len(flags):     # 나아졌을 때만 채택(악화 시 원본 유지)
            spec, flags = spec2, flags2
    spec.selfcheck_flags = flags          # 잔여 경고는 로그용(렌더 미노출)
```
- **반려가 아니다.** 자가검수는 재작성을 유도할 뿐, 통과/탈락 판정은 그대로 `reviewer.py` 가 한다.
- 재작성 호출이 실패해도 **원본 spec 으로 진행** → 파이프라인 정지 없음(`orchestrator.py:145` 는 생성 예외를 시드 SKIP 으로 처리하므로 여기서 raise 하면 안 된다).
- `ADSENSE_SELFCHECK=0` 로 끌 수 있다(OPS 디버그용).

---

## ③ 실측 (LLM 호출 0회)

### 검증 1 — 반려 4건에서 인용된 결함 문장 원문 투입
스크립트: `scratchpad/prove_selfcheck_14.py` (`dist/review/*.json` 의 인용문을 그대로 spec 에 넣음)

| 케이스(원문 출처) | 검출 | 대표 플래그 |
|---|---|---|
| ① `Best Cheap VPS in 2026` + `There is no single cheapest VPS.` | **3건** | `[title-contract] … the body refuses to rank` |
| ② `Bare metal, meanwhile, does not appear in the product overview.` | **1건** | `[negative-claim] Unscoped absence claim` |
| ③ `Workato reported 67% lower cost …` / `$1.91/GPU/hour` / `72 models` | **3건** | `[volatile-figure] Undated figure` |
| ④ `the fetched text` / `Marks and descriptions below` / `in the version reviewed` | **3건** | `[pipeline-language] Remove 'fetched'` |
| ⑤ `Three limits are worth stating outright` / `most comparison articles admit` / `Genuinely` / `This guide takes` / `Read those two paragraphs` | **5건** | `[ai-tell]` |
| **통제군**(5종을 올바르게 쓴 글: 기준 명시 + 지목 + `as of 2026-07-24` 스코프) | **0건** | — |

→ 5종 전부 검출 + 올바르게 쓴 글은 0건 = **요구를 없앤 게 아니라 조건에 붙였다**는 증거.

### 검증 2 — 배선 4케이스 (`scratchpad/prove_wiring_14.py`, `_via_api` 몽키패치)
| 케이스 | 기대 | 실제 |
|---|---|---|
| A 결함 → 개선본 | 2회 호출, 개선본 채택 | ✅ 호출 2회, 제목 `Cheap VPS in 2026: …compared`, 잔여 0건 |
| B 결함 → 악화본 | 원본 유지 | ✅ 잔여 5건(원본), 악화본의 `fetched` 미채택 |
| C 재작성 중 예외 | 원본으로 진행, 미정지 | ✅ `자가검수 재작성 건너뜀(RuntimeError…)` 후 정상 반환 |
| D `ADSENSE_SELFCHECK=0` | 재작성 없음 | ✅ 호출 1회 |

### 검증 3 — 임계값은 발행 28편 실측 분포로 잡았다(추정 금지)
| 지표 | min | median | p75 | max | 채택 임계 | 발화 |
|---|---|---|---|---|---|---|
| 미러 구문 수 | 0 | 0 | — | 4 | `MIRROR_MAX = 3` | 28편 중 **2편** |
| em 대시 단락 커버리지 | 0.00 | 0.31 | 0.42 | 0.62 | `EM_DASH_PARA_RATIO_MAX = 0.60` | 28편 중 **0~1편** |

### 검증 4 — 발행 28편 전수 스캔(스캐너가 무차별 발화하지 않는가)
플래그 0건 **4편** / 1~2건 11편 / 3~4건 9편 / 5건 이상 4편. 유형별 총 발화:
`negative-claim 23 · volatile-figure 20 · ai-tell 16 · title-contract 5 · pipeline-language 4`

무작위 발화가 아니라 **이미 라이브에 나간 실제 결함**을 짚는다(발췌 실측):
- `best-password-managers…`, `bruno-vs-postman`, `otter-ai-vs-fireflies` 본문에 `"was not part of the source material used for this article"` — **파이프라인 언어가 이미 발행돼 있다**(3편, 5회).
- `cursor-vs-windsurf`: `"the table below describes tiers rather than quoting prices"` — 위치 지시.
- `bitwarden-vs-1password`: `"it has no permanent free plan"` — 무스코프 부재 단정.
- `fly-io-vs-railway`: `"dedicated engineer support starts at $29/month"`, `"18 regions"` — 관측일 없는 휘발성 수치.
- `ai-tell` 16건 중 **13건이 `genuine/genuinely`** — ①의 프롬프트 원인과 정확히 일치.

### 검증 5 — 프롬프트 조립 실측
`_user_prompt("best cheap vps 2026", …)` → `TITLE CONTRACT` **포함**, `_user_prompt("hetzner vs digitalocean", …)` → **미포함**(최상급 질의에만 주입, 프롬프트 비대화 방지). 전문은 `prove_selfcheck_14.py` TEST 4 출력.

---

## ④ 부수 조치 — fixture 의 허위 1인칭 경험 주장 제거 (범위 인접, PM 확인 요망)
`generator.py` fixture 에 **실제로 하지 않은 테스트 주장**이 있었다(CLAUDE.md 가드레일·ORDER ⛔ 항목 위반):
```diff
- "In our refactor test, Cursor's repo context reduced manual file-hopping and kept related changes consistent;
-  in day-to-day typing, Copilot stayed out of the way and suggested the next line before we reached for it."
+ "On a repo-wide refactor, Cursor's indexed context is the documented differentiator;
+  for day-to-day typing, Copilot's inline completions are the documented strength."
- "…based on documented features and hands-on use."   →  "…based on each vendor's documented features."
- author_bio="Independent, hands-on software reviews."  →  "Independent software comparisons from official docs and public data."
```
- 현재 노출 범위: `dist/preview/cursor-vs-github-copilot.html` **1건뿐**(`grep -rl` 실측). `dist/queue`·라이브 사이트에는 없음.
- 왜 지금 고쳤나: fixture 경로는 `review_on` 이 False 라 **검수를 타지 않는다**(`orchestrator.py:114`). 즉 이 문장은 검수 없이 빌드될 수 있는 유일한 경로에 있었다.

---

## ⑤ 산출물
- `engine/content/generator.py` — `_SYSTEM` 5종 규칙(578-592) · `_user_prompt` 관측일·제목계약·부재단정·소스언어(429-476) · 스캐너 상수(255-341) · `_prose_units`/`selfcheck`(343-427) · 배선(100-126) · `ContentSpec.selfcheck_flags`(47) · fixture 허위경험 제거
- `config/topics.yaml` — `title_policy` 블록(주석 10-14, 키 15-21). YAML 파싱·기존 키(clusters 5, next_batch_priority 18) 무손상 확인
- `team/reports/2026-07-24-14-content.md` (이 문서)
- 검증 스크립트(스크래치패드, 리포 미포함): `prove_selfcheck_14.py`, `prove_wiring_14.py`

---

## ⑥ DoD 대조
| 완료 기준 | 충족 | 근거 |
|---|---|---|
| 5종 각각 **어디를 어떻게 막았는지** 코드 라인 + 프롬프트 문구 diff | ✅ | 위 ① 표 5개 — 라인번호·실제 문구 전문. 검출은 실측(검증 1) |
| 신규 1편 검수 결과를 **입력 로그와 함께** 제시 | ❌ | **PM 지시로 이번 회차 생성 실행 금지**("코드·프롬프트 수정까지만"). 미실행 = 미충족. 억지 통과 없음 |
| 반려 시 남은 사유가 5종 밖인지 판정 | ❌ | 위와 동일(생성 미실행) |
| ⛔ `reviewer.py`·판정 임계값 무수정 | ✅ | 내가 쓴 파일은 `generator.py`·`config/topics.yaml`·이 보고서 **3개뿐**. `git status` 의 나머지 변경분(`CLAUDE.md`·`guardrails.yaml`·`reviewer.py`·`site_builder.py`·`orchestrator.py`·`.claude/skills/adsense-audit/SKILL.md`)은 내가 손대지 않았다 — 앞 5개는 착수 시점 이미 변경 상태였고, skill 파일은 작업 중 다른 pane 에서 변경됐다. `AI_CLICHE_PATTERNS` 9종 무변경(`len(AI_CLICHE_LABELS)==9`), 검수기 자동반려 규칙 무변경 |
| ⛔ 게이트를 낮추지 않았다 | ✅ | 자가검수는 **추가 요구**만 한다(통과 조건을 뺀 곳 없음). 검출돼도 반려가 아니라 재작성이고, 최종 판정은 그대로 `reviewer.py`. 통제군 0건 = 요구를 조건에 붙인 것이지 없앤 게 아님 |
| ⛔ 자기 검수 통과 선언·발행·배포 | ✅ 미실행 | 큐·`published.json`·`last_publish_date.txt` 무변경. ssh·deploy·`--stage publish` 미실행 |

**종합 판정: partial (done=false).** 5종 차단은 완료·증명, 실검증(1편 생성)만 PM 승인 대기.

---

## ⑦ 이슈 / 리스크
1. **비용·시간**: 발행 28편 중 24편이 1건 이상 플래그 → 실전에서도 **대부분의 생성이 재작성 1회를 유발**할 가능성이 높다. 즉 글 1편당 생성 LLM 호출이 1회 → **최대 2회**(+2~5분). 일일 cap 1편이라 총량은 감당 가능하다고 판단했으나, 20:00 배치 시간이 늘어나는 건 OPS 관측 대상. 부담되면 `ADSENSE_SELFCHECK=0`.
2. **스캐너는 부분망이다(정직하게)**: ⑤의 "manufactured antithesis"·"동일 리듬"은 형태가 다양해 정규식이 일부만 잡는다(A/B 대상 `vercel-vs-netlify` 는 미러 1회로 임계 미달인데 검수기는 지적했다). ⑤의 1차 방어는 프롬프트이고 스캐너는 보조다. ①②③④는 문형이 고정적이라 검출률이 높다.
3. **미검증 가정**: 프롬프트가 실제로 모델 출력을 바꾸는지는 **아직 LLM 으로 확인하지 않았다**(생성 금지 지시 준수). 검증 1은 "결함이 나오면 잡는다"를 증명할 뿐 "결함이 안 나온다"를 증명하지 않는다. → **다음 회차 1편 생성이 반드시 필요**하다.
4. **기존 발행 28편에 실제 결함 잔존**: 파이프라인 언어 3편(5회)·무스코프 부재 단정 다수·무헤지 수치 다수. 이번 조치는 **신규 생성분에만** 적용된다. 기존 글 소급 수정은 별건(REVIEW 감사 + PM 판단) — 특히 `"was not part of the source material used for this article"` 는 독자에게 의미 불명이라 우선순위가 높다고 본다.
5. **정책 리스크 없음**: 트래픽·클릭 생성 코드 없음(G4). 킬스위치·발행 경로 무변경.

## ⑧ 다음 제안 (PM 판단용)
1. **1편 생성 승인** — `best cheap vps 2026`(반려 4건 중 3건의 진원지)로 재시도하면 ①③④를 한 번에 실증할 수 있다. 결과는 `dist/review/<slug>.input.txt`(11-review 가 만든 감사 로그)와 함께 REVIEW 가 판정.
2. **기존 28편의 파이프라인 언어 5회 제거** — 소급 수정 여부 결정 필요(REVIEW 감사 → OPS 재빌드).
3. `title_policy` 를 generator 가 config 에서 읽도록 승격할지 — 지금은 문서·정책 선언이고 집행 로직은 코드 상수다(하드코딩 회피 원칙과의 균형은 PM 판단).

---

# rev 2 — 감사 지적 3종 코드 수정 (2026-07-24 23:30)

> 범위: ORDER rev2 의 **"코드" 항목만**(PM 재지시). rev2 "생성" 항목(신규 1편)은 이번 회차 범위 밖 —
> 선행조건 `12-review rev2`(고지 게이트 구멍 수정) CONFIRMED 후 별도 지시 예정.
> 내가 쓴 파일: `engine/content/generator.py` · `config/topics.yaml` · `dist/review/selfcheck-rev2-proof.{txt,py}` · 이 보고서.
> 검증은 **LLM 실호출 0회**. 재현: `python dist/review/selfcheck-rev2-proof.py`(출력 원본 = `dist/review/selfcheck-rev2-proof.txt`).

## ① 파이프라인 언어 정규식 — 단어 금지 → 문맥 금지 (`generator.py:352-414`)

**원인**: 구 패턴이 **단어**를 금지했다 — `\bfetch(?:ed|ing)\b`, `\b(?:scraped|crawled|parsed|ingested|the corpus)\b`, `\bthe excerpts?\b`.
개발툴 비교 사이트에서 fetch/parse/scrape/crawl/ingest 는 **주제어**다.

**수정**: 금지 단위를 (a) `한정사+파이프라인동사+우리 입력물 목적어` (b) `우리 입력물 목적어+후치 동사`
(c) 그 자체로 입력물을 뜻하는 명사구 (d) 템플릿·마커·렌더러 위치지시 — 네 문맥으로 좁혔다.
목적어 집합은 `source(s)/excerpt(s)/corpus`(`_PIPE_OBJ_SRC:369`)와 `text(s)/material(s)`(`_PIPE_OBJ_TXT:370`)뿐 —
`data/page/response/JSON/schema/copy/draft` 는 **일부러 뺐다**(개발·AI글쓰기 문맥의 정상어).
`text/material` 에는 `provided/given` 을 붙이지 않는다(`_PIPE_VERB_NARROW:367`) — "Jasper rewrites **the provided text**" 오탐 방지.
후치형에서 `used/available` 도 뺐다(`:374` 주석) — "the sources **used** by the crawler" 오탐.
위치지시 명사에서 `list/section/checklist` 제거(`:396`) — 그 블록은 모델이 같은 섹션 html 안에서 순서를 통제하므로 오탐.
반대로 `marked … below` 는 사이 단어 3개까지 허용하도록 **넓혔다**(`:399`) — 반려 3번 초안의 `"marked as absent below"` 를 구 패턴은 놓쳤다.

**실측** (proof P1 — 구 패턴은 rev1 원문을 스크립트에 그대로 복사해 같은 문장에 나란히 돌린 것):

| 입력 | 구(rev1) | 신(rev2) |
|---|---|---|
| 개발 어휘 14문장 **오탐** | **11건** | **0건** |
| 자백 문구 12문장 **미탐** | **3건** | **0건** |

ORDER 지정 오탐 2건: `"Postman displays the parsed response body, and the fetched JSON is cached…"` 구 2건 → 신 0건 /
`"Scraped data can be exported to CSV…"` 구 1건 → 신 0건. 추가 오탐 사례(전부 신 0건):
`git fetch`, `each crawled page`, `keeps fetching the schema … is parsed`, `ingested 10,000 rows`, `The excerpt view in Ghost`,
`the parsed stack trace`, `the provided text`(Jasper), `the copy provided`(Copy.ai), `the sources used by the crawler`, `the fetched data`(n8n).

ORDER 지정 4문구는 전부 계속 잡힌다: `the fetched text` ✅ / `in the version reviewed` ✅ / `sources supplied` ✅(구는 **미탐**) / `source material` ✅.
**구가 놓치던 3건을 새로 잡는다**: `The sources supplied…` · `Bare metal is marked as absent below.` ·
`Specific plan prices weren't in the reviewed material.`(← 발행분 `best-password-managers…` 본문 실측).

발행 28편 코퍼스에서 파이프라인 발화 4 → **6건**, 6건 전부 눈으로 확인한 **진짜 자백**이다(오탐 0):
`source material` 5회(3편) + `used for this article` + `in the reviewed material` + `the table below`(cursor-vs-windsurf).

## ② 셀프체크 게이밍 — 토큰이 아니라 근거를 본다

감사 실측(토큰 3개 덧대기로 7건 → 0건)에 대해 **(a) 피드백 분리 + (b) 근거 동반 확인**을 둘 다 넣었다.

**(a) 플래그 문구를 재작성 피드백에 그대로 노출하지 않는다.**
`selfcheck_detail():567` 이 `(kind, quote, note)` 를 돌려주고 — 로그용 문자열은 `selfcheck():631`,
프롬프트용 문구는 `_rewrite_feedback():551` 이 따로 만든다(`_REWRITE_GUIDANCE:532`, `_REWRITE_PREAMBLE:528`).
피드백은 **초안 자신의 문장만 인용**하고 지침은 일반화한다. 배선 `generator.py:113-124`.
- proof P3 실측: 탐지 토큰 10종(`as of`·`how we chose`·`selection criteria`·`our pick`·`we recommend`·`methodolog`·`checked on` 등)
  **피드백 등장 0종**, 플래그 문자열이 피드백에 그대로 포함 **0건**.
- 프리앰블이 "이건 검사기 힌트이지 판정이 아니다 / 문구를 덧대 만족시키지 말라"를 명시한다.

**(b) 근거 동반 확인 4곳** — 표면 토큰으로는 지워지지 않는다.

| 검사 | 구(토큰만 봄) | 신(근거를 봄) |
|---|---|---|
| 휘발성 수치 | `as of` 문자열 유무 | **grounding(실제 페치 소스)에 그 수치가 있는가**(`_unsupported_figures:471`, `_numeric_values:460`). 없으면 날짜를 붙여도 계속 플래그 |
| 기준 명시 | `CRITERIA_RE` 매치 | 매치 단락이 **실질 분량(25단어↑)** 인가(`_criteria_unit:489`) → `"How we chose: price and features."` 한 줄은 불인정 |
| 지목 | `PICK_RE` 매치 | 결론부에 **구조화 필드의 실제 제품명**이 등장하는가(`_product_names:497`·`_named_pick:516` — comparison/feature_matrix/pros_cons 에서 추출, 플랜명 `pricing[].name` 은 제외) |
| 부재 단정 | `SCOPE_CUE` 하나(날짜 포함) | **무엇을 봤는가(페이지·문서) ∧ 언제 봤는가(날짜)** 둘 다(`SCOPE_CUE_RE:336` 에서 날짜 단서 제거, `selfcheck_detail:598`) |

**실측**(감사자 스크립트 `audit_gaming.py` 를 **수정 없이** 그대로 재실행):
`A 결함 원본 7건 → B 토큰만 덧댐 3건`(rev1 은 `7건 → 0건`). 남은 3건 = 기준 미실질 1 + 부재 단정 무근거 2.
`제품명 미지목` 판(`Our pick is clear.` + comparison a/b 존재) → 지목 검사 계속 발화(proof P2 C).
**grounding 대조 실측**(proof P2):

| 문장 | grounding 없음 | grounding 있음 |
|---|---|---|
| `Hetzner's CX22 costs $4.50 … as of 2026-07-24` (소스엔 EUR 4.51) | 0건 | **1건** `figures absent from the cited material: $4.50, $9` |
| `… listed CX22 at EUR 4.51 … as of 2026-07-24` (소스와 일치) | 0건 | **0건** |

⚠️ **정직하게**: 완전 면역이 아니다. grounding 이 없는 경로(페치 실패·fixture)에서는 수치 근거 대조가 꺼지고 날짜 토큰만 남는다.
셀프체크는 게이트가 아니라 보조 신호이므로(코드 주석 `generator.py:249-259` 에 명시) 최종 방어선은 그대로 `reviewer.py` 다.

## ③ `config/topics.yaml` `title_policy` — 실제로 배선 (`generator.py:264-303`, `638-667`)

`title_policy():282` 가 config 를 읽어(파일 없음·YAML 손상 시 기본값 폴백) 두 곳을 결정한다:
- **프롬프트**: `_title_contract_block():658` 이 `superlative_requires` 각 항목 → 요구사항 줄, `otherwise` → (b) 줄, `never` → 마지막 경고 줄을 조립. `_user_prompt:690` 에서 주입(하드코딩 문구 삭제).
- **스캐너**: `stated_selection_criteria` → 기준 검사, `named_pick_per_use_case` → 지목 검사, `never` → 순위거부 검사 활성화(`selfcheck_detail:578-592`).
- `opinion_framing` 은 **프롬프트 전용**(스캐너 검사 없음) — 토큰 검사를 붙이면 상투구 덧대기를 유발하므로 의도적으로 두지 않았고, config 주석에 그대로 적었다.
- config 에 **있는 키는 그대로 반영**(빈 값 = 규칙 해제), 없는 키만 기본값(`:290-293`).

**실측**(proof P4 — 실제 `config/topics.yaml` 을 수정 → 측정 → 복원):

| config | 정책 | selfcheck | 프롬프트 |
|---|---|---|---|
| 원본 | requires 3 · never 설정됨 | **5건** | TITLE CONTRACT **4줄** |
| `stated_selection_criteria` 제거 + `never:` 비움 | requires 2 · never `''` | **3건**(기준·순위거부 검사 소멸) | **3줄**, (a) 줄에서 기준 문구 소멸 |
| 복원 | 원상 | **5건** | 4줄 |

복원 확인: `파일 동일: True`. `git diff --numstat config/topics.yaml` = `23  0`(rev1 18줄 + rev2 주석 5줄, 삭제 0).
YAML 파싱·기존 키 무손상(clusters 5 / next_batch_priority 18).

## ④ AI 리듬 임계값 재검토 (`generator.py:416-423`)

28편 실측 분포(미러 구문 수): `0:19편 · 1:5 · 2:2 · 3:1 · 4:1`. 임계 3 = 2편(7%) · 임계 2 = 4편(14%).
검수기는 미러 **1회**인 글(`vercel-vs-netlify`)도 지적했다 → 정규식이 검수기 감도를 따라잡을 수 없다.
그래서 **주 방어선은 프롬프트(_SYSTEM VOICE 불릿), 최종 판정은 reviewer.py 의 ai_tells(자동반려)** 임을 코드 주석에 명시하고,
임계는 `MIRROR_MAX 3 → 2`(`:422`)로 한 칸만 낮췄다 — 미탐 1건의 비용(생성+검수 1사이클 폐기) > 오탐 1건의 비용(재작성 1회)이고,
2 이상은 여전히 상위 14%라 "대부분의 글을 자동 재작성"시키지 않는다.

## ⑤ 코퍼스 영향·무결성 (proof P5·P6)

발행 28편 유형별 발화: `negative-claim 22→23 · title-contract 8→8 · pipeline-language 4→6 · ai-tell 16→18 · volatile-figure 20→20`
(≥1건 24편 → 25편). 증가분은 위에서 확인한 **진짜 지적**이고, 감소분(개발 어휘 오탐)은 이 코퍼스엔 원래 없었다 —
오탐은 앞으로 쓸 주제(`best api testing tools`·`bruno vs postman`·`how to self-host n8n`·AI 글쓰기 툴)에서 터질 것이었다.

무결성: `AI_CLICHE_PATTERNS` **9종 그대로**, `scan_ai_cliches` 동작 동일, fixture 렌더 스모크 정상(html 36,213자 / blocks 9 / 잔여 플래그 0).
rev1 배선 테스트 4케이스(`prove_wiring_14.py`: 개선채택·악화거부·예외내성·`ADSENSE_SELFCHECK=0`) **전부 재통과**.

## ⑥ DoD 대조 (rev2 코드 항목)

| rev2 코드 항목 | 충족 | 근거(실측) |
|---|---|---|
| 파이프라인 정규식을 문맥 기반으로 좁힘 | ✅ | 오탐 11 → **0** / 미탐 3 → **0** (proof P1) |
| 셀프체크 게이밍 대응 (a) 또는 (b) | ✅ (둘 다) | 피드백 토큰 노출 0종·플래그 원문 0건(P3) · 감사 케이스 7→0 이던 것이 **7→3** · grounding 대조 발화(P2) |
| 셀프체크는 게이트 아님을 주석 명시 | ✅ | `generator.py:249-259` |
| `title_policy` 배선 또는 제거 | ✅ 배선 | config 수정 → 플래그 5→3 · 프롬프트 4→3줄, 복원 확인(P4) |
| AI 리듬 임계값 재검토 또는 명시 | ✅ (둘 다) | 분포 실측 + 주석 명시 + `MIRROR_MAX 2` |
| ⛔ `reviewer.py`·판정 임계값 무수정 | ✅ | 내가 쓴 파일 4개뿐(generator.py · topics.yaml · dist/review/proof 2 · 본 보고서). `AI_CLICHE_PATTERNS` 9종 불변 |
| ⛔ 검출을 줄여 오탐만 없애지 않았다 | ✅ | 미탐 3→0(새로 잡는 자백 3문형) · 코퍼스 발화 총량 **증가**(70→75) · 게이밍 내성 상승 |
| ⛔ 큐·발행·배포 미실행 | ✅ | `stage_publish`·`deploy.py`·`daily_local.ps1`·ssh 미실행. `dist/queue`·`published.json`·`last_publish_date.txt` 무변경 |

## ⑦ 남은 리스크 (정직하게)

1. **잔존 오탐 가능성**: `the supplied text` 류는 AI 글쓰기 툴 글에서 사용자 입력을 가리킬 수 있다(현 패턴은 발화). 보조 신호라 비용은 재작성 1회.
2. **잔존 게이밍 경로**: grounding 이 비어 있는 생성(페치 실패·fixture)에서는 수치 근거 대조가 꺼진다 → 날짜 토큰만으로 통과 가능. 구조적 해법("그라운딩 없으면 수치 금지")은 발행 가능 주제를 크게 줄이므로 PM 판단 사항으로 남긴다.
3. **미검증 가정**: 프롬프트·피드백 변경이 실제 모델 출력을 어떻게 바꾸는지는 여전히 **LLM 으로 확인되지 않았다**(rev2 생성 항목은 선행조건 대기). 이번 증명은 "결함이 나오면 잡고, 정상 어휘는 안 잡는다"까지다.
4. **정책 리스크 없음**: 트래픽·클릭 생성 코드 없음(G4), 킬스위치·발행 경로 무변경.

## ⑧ 다음 (PM 판단)

1. `12-review rev2` CONFIRMED 후 **신규 1편 생성**(rev2 생성 항목) — `best cheap vps 2026` 권장(①③④를 한 번에 실증).
2. 기존 발행분 소급: 파이프라인 자백 6건(`source material` 5 + `in the reviewed material` 1) — REVIEW 감사 → OPS 재빌드 여부 결정.

---

# rev 2 — 생성 결과 (2026-07-25 00:05, ORDER rev2 "생성" 항목)

> 실행: `python dist/review/ORDER14-gen-one.py --keyword "best cheap vps 2026" --cluster hosting-selfhost`
> 재현·증거: `dist/review/ORDER14/`(run.log · run.json · spec · html · review.json · review.log) +
> `dist/review/<slug>.input.txt`(검수기가 실제로 본 입력) + `dist/review/<slug>.json`(판정).
> **LLM 실호출 5회** — URL 발견 1 · 생성 1 · 자가검수 재작성 1 · 검수 1 · stdin 전송 확인용 1단어 프로브 1.
> ⛔ 발행 경로 무접촉: `stage_publish`·`deploy.py`·`daily_local.ps1`·ssh 미실행. **큐에도 넣지 않았다**(사유 ⑥).

## ① 결과 한 줄

**생성 성공 · 품질게이트 통과 · 검수 반려(passed=false, severity=medium, issues 6, ai_tells 5).**
반려 사유는 **5종 중 ⑤(AI 티) 하나가 사실상 전부**다. ②③④는 차단 성공(검수기가 독립 확인),
①은 **제목에서는 차단됐으나 최상급이 본문(TLDR/verdict)으로 이동**해 부분 재발했다.
그리고 **차단 실패보다 먼저 보고해야 할 인프라 결함**을 하나 찾았다(⑤ 항목 — WinError 206).

| 항목 | 값 |
|---|---|
| 시드 | `best cheap vps 2026` (미발행 백로그 **1위**, score 0.712 — 임의 선정 아님) |
| slug | `cheap-vps-hosting-in-2026-how-to-choose-with-hetzner-cloud-and-digitalocean-compared` |
| 제목 | `Cheap VPS hosting in 2026: how to choose, with Hetzner Cloud and DigitalOcean compared` |
| provider / model | `claude_cli` / `claude-opus-4-8` (API 키 없음) |
| 그라운딩 | 공식 소스 **5개 / 15,636자** 페치 성공 |
| 산문 | 2,465 단어 / 58 단락 |
| quality_gate | **passed=True** (사유 없음) |
| reviewer | **passed=False · severity=medium · issues 6 · ai_tells 5** |
| 검수 입력 로그 | `dist/review/cheap-vps-hosting-in-2026-how-to-choose-with-hetzner-cloud-and-digitalocean-compared.input.txt` (34,534자, sha16 `a27c5154babcc167`) |

## ② 5종 판정 — 요구된 대로 하나씩

### ① 최상급 제목 미이행 — 🟡 **제목 차단 성공 / 본문으로 이동(부분 재발)**
- **성공한 부분**: 시드가 `best cheap vps 2026`(최상급)인데 모델이 **(b) RETITLE 을 실제로 선택**했다.
  실측 — `SUPERLATIVE_TITLE_RE.search(title)` = **None**, 순위거부 문형 **0건**, `_named_pick` = **True**.
  반려 4건 중 3건의 진원지였던 "제목은 순위를 약속, 본문은 순위 거부"는 **재발하지 않았다.**
  검수기도 독립적으로 확인: *"the title matches the content"*.
- **실패한 부분**: 최상급이 TLDR 로 이동했다 — `Cheapest path to a self-managed Linux box … Hetzner Cloud` /
  `Best value once you need managed pieces: DigitalOcean`. 이게 이번 반려의 **유일한 legal 지적**이고,
  검수기 총평이 그대로 짚는다: *"an article that deliberately refuses to quote prices should not be ruling on which vendor is cheaper."*
- **원인(코드 라인)**: `generator.selfcheck_detail():581` 의 `sup = SUPERLATIVE_TITLE_RE.search(title)` — 제목계약
  검사 **3종 전부가 `if sup:` 안에 있다**. 즉 (b) retitle 을 고르는 순간 기준명시·지목·순위거부 검사가 **동시에 꺼진다**.
  실측 증거: 이 글은 `_criteria_unit` = **없음**(선정 기준을 밝힌 실질 단락이 없다)인데 플래그는 **0건**이었다.
  `opinion_framing`(판단으로 표현)은 애초에 **프롬프트 전용·스캐너 없음**(config/topics.yaml:23-24 주석).
  → **제목을 고치면 본문의 순위 주장은 무검사**가 된다. 이건 rev2 배선의 설계 구멍이지 모델의 일탈이 아니다.

### ② 경쟁사 단정적 부정 주장 — ✅ **차단 성공**
- 스캐너 `ABSENCE_CLAIM_RE ∧ ¬(SCOPE ∧ DATE)` = **0건**.
- 검수기 독립 확인(총평 원문): *"No fabricated quote and **no 'X lacks Y' claim about either vendor**."*
- 🟡 잔여(약형): `Auction hardware is older by design, so weigh the warranty and replacement story` —
  소스에 없는 **부정적 성격규정**. "X에 Y가 없다" 문형이 아니라 정규식이 못 잡는다. 형태가 바뀐 인접 결함.

### ③ 휘발성·제3자 수치 무헤지 — ✅ **차단 성공**
- 스캐너 = **0건**(소스 없는 수치 0 · 관측일 없는 수치 0). 산문 내 통화·퍼센트 토큰 3개 전부 처리됨.
- **ORDER 가 이름으로 지목한 `Workato 67%` 가 실제로 이 글에 나왔고**, 검수기가 통과시켰다 —
  *"the Workato 67%, Character.ai 2x and Hippocratic AI 40% P99 figures are **explicitly labelled the company's own marketing claims** and expressly discounted for small-server decisions"*. 프롬프트 문구가 의도대로 작동한 직접 증거.
- 🟡 잔여(형태 변화): 수치를 뺀 자리에 **무근거 정성 가치주장**이 남았다 — `more RAM and disk for the money`,
  `cheap adjacent pieces from the same menu`. factual 지적 3건 중 2건이 이것. 숫자가 없어 `VOLATILE_FIGURE_RE` 사각지대.

### ④ 파이프라인 언어 노출 — ✅ **차단 성공**
- 스캐너 13문형 = **0건**. 검수기 지적 = **0건**. rev2 의 문맥기반 정규식이 오탐도 내지 않았다(개발 어휘 다수 포함 글인데 재작성 유발 0).

### ⑤ AI 티 — ❌ **재발 (차단 실패)**
- 생성측 셀프체크가 **2건 검출 → 1회 재작성 → 여전히 2건**(개선 실패, `len(details2) <= len(details)` 로 재작성본 채택):
  `counted-list section opener — "Three situations"` · `the 'X, not Y' antithesis appears 4 times`.
- 검수기는 **5건**을 잡았다(에피그램 종결, `X rather than Y` ~7회, 삼단 수사, 동일 열거 스캐폴드 2연속, 생성된 위트형 소제목).
- 그리고 이게 **반려를 만든 유일한 규칙**이다 — 검수기 LLM 자신의 총평은
  *"No high-severity defect found, **so this passes the publish gate** with the fixes above applied."*
  즉 LLM 은 passed=true 를 냈고, `reviewer.py:382-385`(ai_tells 하나라도 있으면 자동 반려)가 뒤집었다.
- **정직한 결론: 오늘 글이 막힌 이유는 ⑤ 하나다.** rev1·rev2 에서 내가 이미 "⑤의 1차 방어선은 프롬프트,
  스캐너는 부분망"이라고 적었는데(rev1 리스크 2), **프롬프트 방어선이 실제로 뚫렸다**는 것이 이번 실증의 결과다.
  `_SYSTEM` VOICE 불릿은 (a)(b)(c)(d)로 이 4형태를 이미 명시 금지하고 있다 — 명시했는데도 나왔다.

### 5종 요약
| | 결함 | 판정 | 근거 |
|---|---|---|---|
| ① | 최상급 제목 미이행 | 🟡 부분 재발(제목→본문 이동) | 제목 최상급 0·순위거부 0·지목 O, 그러나 TLDR 최상급 → legal 1건 |
| ② | 경쟁사 단정 부정 | ✅ 차단 | 스캐너 0 + 검수기 "no 'X lacks Y' claim" |
| ③ | 수치 무헤지 | ✅ 차단 | 스캐너 0 + Workato 67% 를 마케팅 주장으로 명시 처리 |
| ④ | 파이프라인 언어 | ✅ 차단 | 스캐너 0 · 검수기 0 · 오탐 0 |
| ⑤ | AI 티 | ❌ **재발** | 셀프체크 2(재작성 후에도 2) · 검수기 5 · **반려 확정 사유** |

## ③ 5종 **밖**의 새 사유 — 2건 (ORDER 요구 판정)

1. **[coherence] 인용했으나 본문에 쓰지 않은 소스.** `https://www.digitalocean.com/pricing/droplets` 가 sources 에
   있는데 그 페이지의 사실(초 단위 과금 등)이 본문에 없다. **구조적 원인은 내 소유 코드**다 —
   `generator._finalize_sources():196-200` 이 **페치한 URL 전부를 무조건 sources 에 추가**한다(모델이 썼든 안 썼든).
   실측: 페치 5개 = sources 5개, SOURCE 5 는 3,500자를 온전히 받았는데도 미사용. → 5종에 없는 **새 사유**.
2. **[factual] 무근거 시장·인기 주장.** `two providers that small teams shortlist constantly` — 소스에 없는
   시장 점유·인기 서술을 배경 사실처럼 제시. 5종 어디에도 해당 없음 → **새 사유**.

## ④ 반려 전문 (검수기 원문 요지)

```
passed=False  severity=medium  issues=6  ai_tells=5   (monetized=false → 고지 지적 0건, 강등 0건)
[legal]     TLDR 의 절대 최상급("Cheapest path…", "Best value…")을 사실로 진술 — 가격을 일부러 인용하지 않는 글이 어느 쪽이 싸다고 판정
[factual]   FAQ "Is Hetzner cheaper than DigitalOcean?" 의 가치 판단이 소스 범위를 넘음
[factual]   "Auction hardware is older by design" — 인용 페이지에 없음 + 벤더 제품에 대한 부정적 성격규정
[factual]   가격이 없는 근거 위에 세운 가격·가치 주장("more RAM and disk for the money" 등)
[coherence] sources 의 DO droplets 가격 페이지가 본문에 전혀 사용되지 않음
[factual]   "two providers that small teams shortlist constantly" — 무근거 인기 주장
```
검수기가 **명시적으로 통과 처리한 것들**(= 5종 차단이 먹힌 증거): 조작 인용 0 · `X lacks Y` 0 ·
Workato 67%/Character.ai 2x 를 마케팅 주장으로 라벨 · **허위 1인칭 경험 0**("no testing, benchmarking or
hands-on language anywhere") · AdSense 정책 무결(금지주제·클릭유도·기만 0) · 상표 사용 정당 · 비YMYL.

## ⑤ 🔴 **최우선 보고 — 검수기가 Windows 에서 호출 자체를 못 한다 (WinError 206)**

1차 시도의 검수는 판정을 내지 못하고 **`FileNotFoundError: [WinError 206] 파일 이름이나 확장명이 너무 깁니다`** 로 죽었다.

- **원인**: `generator.complete_text():737` 은 프롬프트를 **argv** 로 넘긴다. Windows `CreateProcess` 의
  명령줄 한도는 **32,767자**. 이번 검수 프롬프트 실측 **36,568자 → 3,801자 초과**.
- **claude 와 무관함을 증명**(실행파일을 python 자신으로 바꿔 재현):
  `argv 총길이 32,075 → OK` / **`32,775 → WinError 206`** / `40,075 → WinError 206`. 정확히 OS 한도다.
- **언제부터?** `reviewer.py` mtime = **2026-07-24 23:13**(11-review rev2). 그 ORDER 가 검수 입력 상한을
  **12,000→40,000 · 소스 5,000→20,000** 으로 올렸다 — **그 인상 자체는 옳다**(검수기가 생성기보다 적게 보던
  구멍을 막은 것). 그런데 부작용으로 **정상 길이 + 그라운딩 글의 검수 호출이 OS 한도를 넘게 됐다.** 32분 뒤 내 실행이 처음 밟았다.
- **영향 범위 실측**(발행 31페이지 평문 길이 분포 median 13,217 · max 16,698 기준):

  | 그라운딩 | 32,767 초과 글 |
  |---|---|
  | 0자(그라운딩 실패 시) | 0/31 |
  | 8,000자 | 0/31 |
  | **15,636자(이번 실측치)** | **10/31** |
  | 17,500~20,000자(config 상한 `5 × 3,500`) | **28/31** |

- **왜 치명적인가**: orchestrator 는 이 예외를 `REVIEW 실패→미발행` 한 줄로 삼키고 `break` 한다
  (`orchestrator.py:216-218`) → **초안은 만들어졌는데 큐 0편**. 그리고 글 생성의 **유일하게 작동하는 경로가
  로컬 Windows** 다(서버 CLI 미인증, 03-ops). 즉 **오늘 이후 로컬 배치는 대부분의 글에서 이 지점에서 죽는다.**
- **나는 고치지 않았다.** `reviewer.py` 는 REVIEW 소유이고 ORDER 경계가 명시 금지다. `complete_text` 는
  내 소유(generator.py)지만 **검수 전송 방식은 REVIEW 판정에 직결**되므로 임의 변경 대신 보고한다.
  판정을 받아내기 위해서만 **하네스에서 전송 방식을 우회**했다(`dist/review/ORDER14-review-one.py`):
  같은 system/user 문자열을 argv 대신 **stdin** 으로 전달. 판정 로직은 전부 `reviewer.review()` 가 수행 —
  프롬프트·임계값·고지정책·ai_tells 규칙 무변경. 증명: user sha16 `a27c5154babcc167` 가
  **1차 실패 때 저장된 `input.txt` 와 바이트 동일**(`identical: true`).
- **권고(REVIEW/OPS 판단)**: `complete_text` 의 claude_cli 분기를 **stdin 전송**으로 바꾸면 한도가 사라진다
  (실측 rc=0). 단 `stdin=DEVNULL` 은 콘솔 없는 환경(20:00 Task Scheduler)에서 CLI 즉사를 막으려 일부러 넣은
  것이므로(generator.py:918-920), **stdin 을 프롬프트 파이프로 쓰는 변경은 무콘솔 환경에서 반드시 재실측**해야 한다. OPS 검증 대상.

## ⑥ 왜 큐에 넣지 않았나 (경계 판단)

검수 반려라 어차피 큐 대상이 아니지만, 통과했더라도 넣지 않았을 것이다:
`human_gate.py:3-5` 가 명시하듯 **`site_builder.build()` 는 `dist/queue/*.html` 을 그대로 라이브에 반영한다**
(글마다 스킵 로직 없음). 즉 큐 기록 = 다음 build/deploy 에서 자동 발행 = **G1 우회**.
→ 산출물은 `dist/review/ORDER14/<slug>.a1.html` 에만 뒀다. 발행은 REVIEW pass + 사람 승인 + OPS 실행.

**무접촉 증명**(하네스가 실행 전후 해시 비교, `run.json.guard`):
`published.json` `10b767f583c922f5` → 동일 · `last_publish_date.txt` `bc0d014812921636` → 동일(여전히 2026-07-19) ·
`dist/queue` 28파일 목록 해시 `3433b6266fcee708` → 동일. `unchanged=True`.

## ⑦ DoD 대조 (ORDER rev2 생성 항목)

| 기준 | 충족 | 근거 |
|---|---|---|
| 신규 1편을 **끝까지 생성** | ✅ | 생성·품질게이트 통과·검수 판정 수령. 산출물 `dist/review/ORDER14/*.a1.{html,spec.json}` |
| 검수 결과를 `dist/review/<slug>.input.txt` 와 함께 제시 | ✅ | input.txt 34,534자(sha `a27c5154babcc167`) + 판정 `dist/review/<slug>.json` + `ORDER14/review.json` |
| 반려 시 **5종 밖 새 사유인지 판정** | ✅ | ⑤ 재발(차단 실패, 숨기지 않음) · ① 부분 재발 · ②③④ 차단 · **새 사유 2건**(미사용 인용 소스 · 무근거 인기 주장) |
| ⛔ `stage_publish`·`deploy.py`·`daily_local.ps1` 미실행 | ✅ | 미실행. 큐·published.json·last_publish_date 해시 불변 |
| ⛔ 검수기·임계값 무수정 | ✅ | `reviewer.py` 무수정(mtime 23:13, 내 작업 이전). 이번 회차에 `generator.py`·`topics.yaml` **도 수정하지 않았다** — 내가 쓴 파일은 `dist/review/ORDER14*` 와 이 보고서뿐 |
| ⛔ 게이트를 낮추지 않았다 | ✅ | 판정은 전부 `reviewer.review()` 원본. 우회한 것은 **프로세스 기동 방식**뿐이고, 그 결과는 **반려**다(통과율을 올리는 방향으로 쓰지 않았다) |

## ⑧ 남은 이슈 / 다음 (PM 판단용)

1. 🔴 **WinError 206 (최우선)** — 고치지 않으면 로컬 배치가 계속 0편. REVIEW/OPS 소관. ⑤ 참조.
2. ❌ **⑤ AI 티: 프롬프트 방어선 실패 확인.** `_SYSTEM` VOICE 가 명시 금지한 4형태가 그대로 나왔고,
   1회 자가 재작성도 못 줄였다(2→2). 다음 수는 프롬프트 문구 추가가 아니라 **재작성 루프 강화**(1회 → 개선될 때까지 N회,
   또는 ai-tell 만 별도 패스로 다시 쓰기)라고 본다. 비용 증가는 PM 판단.
3. 🟡 **① 검사 활성 조건 결함** — 제목계약 3종이 `if sup:`(제목 최상급) 안에만 있어 **retitle 을 고르면 전부 꺼진다**.
   본문(TLDR/verdict)의 최상급도 트리거로 넣는 것이 맞다. 내 소유 코드 수정이라 **다음 ORDER 승인 시 즉시 가능**.
4. 🟡 **`_finalize_sources` 무조건 추가** — 페치했으나 안 쓴 페이지가 인용으로 남아 coherence 반려를 만든다.
   "본문에 근거가 등장한 소스만 남긴다" 로 좁히는 것이 맞다. 역시 내 소유·다음 ORDER 대상.
5. ℹ️ **오탐 회귀 없음**: rev2 가 고친 파이프라인 정규식이 개발 어휘 다수인 이 글에서 **재작성 0회** 유발.
6. ℹ️ **정책 리스크 없음**(G4): 트래픽·클릭 생성 코드 없음. 킬스위치·발행 경로 무변경.
