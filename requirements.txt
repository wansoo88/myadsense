# pjt12-adsense 자동화 엔진 의존성
pyyaml>=6.0            # config/*.yaml 로드
httpx>=0.27           # API 호출 (AdSense/Search Console/PageSpeed)
google-api-python-client>=2.0   # AdSense Management API / Search Console API
google-auth>=2.0
google-auth-oauthlib>=1.0
anthropic>=0.40       # Claude API (콘텐츠 생성)
datasketch>=1.6       # MinHash 근접 중복 탐지 (quality_gate)
python-dotenv>=1.0    # .env 로드
# 발행 어댑터(택1):
#   wordpress → 표준 httpx로 REST API 호출 (추가 의존성 없음)
#   static_ssg → GitPython>=3.1
